# Cashback System — UI/UX Design To-Do (Handoff)

**Product:** RRQ Topup (www.rrqtopup.com) · program name = **RRQSHOP Rewards** · unit = **`poin`** (lowercase) — per the copy law, HANDOFF §2; "RRQ Shop Rewards" was the drift this doc carried until 2026-08-20
**Scope:** Customer-facing site only · **Started:** 2026-07-02 · **last worked: 2026-08-20** — see the 08-20 block at the top of NEW SESSION · earlier: status snapshot v5 + the 07-31 and 08-20 session logs (before "Top design risks")
**Context:** Guest checkout exists · full member area exists · PRIME reposition included

**Companion docs (all current):** **[HANDOFF.md](HANDOFF.md) — the front-end handoff: the laws (colour/copy/compliance/a11y/motion), surface-by-surface status, error & edge states, known mock defects, the full Figma node index and the colour styles. THIS DOC plans the work; HANDOFF.md specifies it. Keep both in step — they were both corrected on 2026-08-20.** · [publisher-compliance-copy-rule.md](publisher-compliance-copy-rule.md) — price-claim rule + live-site audit + CI lint · [prime-landing-figma-vs-prod.md](prime-landing-figma-vs-prod.md) — PRIME landing Figma-vs-prod copy delta · [rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md) — hero motion FE handoff · [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md) — header button motion (⚠️ orphaned by the 07-31 nav decision) · [cashback-research-R1-R3.md](cashback-research-R1-R3.md) — R1–R3 research (R3 guest escrow parked).

**Shippable code in this repo root:** `rewards-hero-*` (5 files — hero motion, not integrated) · **`email-E2-*` (7 files — E2 email, complete and ready to send)**.

---

## ⏩ NEW SESSION — START HERE

> **📐 HOW THIS DOC RELATES TO [HANDOFF.md](HANDOFF.md) (rule set 2026-08-20).**
> **This doc plans the work. HANDOFF specifies it.**
> - **Single source of truth for node IDs, colour values and the laws = HANDOFF** (§9 node index, §2 laws, §1 mechanics). This doc references **style names** (`Rewards 4`, `Primary`, `Rewards Surface 6`) and **item numbers** (#20, C4, E2) instead of restating hexes and IDs.
> - **Single source of truth for what is left to do, why it was decided that way, and what was rejected = this doc** (phases, item numbers, session logs, parked items).
> - Node IDs still appear inside **session logs** on purpose — that is an append-only record of what was built when. If a session log and HANDOFF §9 ever disagree, **HANDOFF wins**.
> - Deduped 2026-08-20 after a rule that was wrong in three places at once (both docs plus a Figma set description) took three separate corrections to remove.


**▶ SESSION 2026-08-20 — DESKTOP PARITY, COLOUR STYLES, AND ONE RETRACTION.**

**Built (all on page "RRQSHOP Rewards", file `RWkIM1Q8o5pqt6eK3O8WBe`):**
1. **`Rewards / Desktop`** `1873:26031` — the nominal banner, desktop. 4 variants, same props as mobile `451:14444` so instances swap 1:1. 793×36, radius `12 12 0 0`, content **centred** (mobile is left-aligned with the Login pill pushed right) because desktop banners are the **top edge of the step-1 card**, not a floating strip. The owner's sample frame inside `Game Page` `1869:31245` was replaced by a live instance `1875:14181`.
2. **`Pilih Pembayaran / Desktop`** `1888:26536` — 5 states (`on` `1888:26058` · `off` `1888:26172` · `non-redeemable` `1888:26289` · `coins-only` `1888:26397` · `payment-extra` `1888:26418`). **Built twice:** the first attempt cloned the game-page step-2 panel `1869:31735` and was thrown away — that panel is a flat hazard-stripe grid matching neither production nor mobile. Correct rule (owner): **desktop is mobile's accordion with rows two-per-line**, nothing else. Wrap auto-layout 736 wide, rows 362, `primaryAxisAlignItems=MIN` so a lone 5th row sits left.
3. **`Konfirmasi Pesanan - D (modal overlay)`** `1894:26082` — **no desktop variant set exists or should.** Every confirmation frame in the 2.0 file is 375 wide, including the one inside the 1440 desktop frame `164:7734` (modal at x533, dead centre). Desktop already renders the same card centred over a scrim, so `587:12570` is used as-is on both breakpoints; only the overlay was missing.
4. **Colour styles — 15 local, as an ADDITION to the 2.0 library.** The library (`Qge4kS2ihFEm4900iZgzkD`, palette `383:12517`) publishes 18 flat styles and they are **already bound throughout the 2.5 file** as REMOTE styles. So six styles created here were **deleted as duplicates**: Gold (=`Primary`), Gold Gradient (=`Primary Gradient`), Boost Green (=`Success`), Spend (=`Surface 7`), Expired (=`Surface 6`), Destructive (red conflict, below). What remains: **`Rewards/Rewards 1–5`** (the purple scale, dark→light — the library has no purple at all) · **`Rewards/Surface 1–7`** (tinted grounds: 1–3 poin · 4–5 boost · 6–7 PRIME) · `Rewards/Green Highlight` · `Rewards/Near Expiry` · `Rewards/On Gold`. Swatch board `1911:26193`. **Nothing is bound to them yet** — the rebind sweep is the next real step, and it would also fix the PRIME-tile defect (`Rewards/Surface 6` is the correct ground for rows currently on `Rewards/Surface 3`).

**🚨 RETRACTED: the DANA × poin-ON rule.** It never existed. Per the owner: redeem eligibility is a **per-product/game backend flag** (DANA was only an example of a non-eligible product), and **no payment method is barred from combining with poin**. It had hardened into the Figma set description as "DANA RULE (locked)" and into 12 places in HANDOFF.md. Undone in mobile `631:12571`, desktop `1888:26058` and the `632:12570` description. The *mechanism* survives, re-instanced on the true case: **a method minimum tested against the remainder after poin**.

**Then production overturned two of my own conclusions** (owner screenshots): (a) the below-minimum treatment is **already live** — dim row + persistent `Minimal Rp 10.000` sub-line + **price retained** — so "the price is gone, not restated" was the anomaly, and the mock's OVO row is faithful; (b) production shows **all four groups expanded** in that state, so the "signal on the collapsed Virtual Account header" I proposed is unnecessary. The sub-line **is** the reason; no event copy needed.

**▶ NEW OPEN ITEMS from 08-20 (none are design-blocked except the first):**
- 🚨 **The mock has no Virtual Account or Convenience Store rows at all** — only group headers. Those 13 rows (11 VA + Alfamart + Indomaret) are the **only** carriers of a minimum, which is why the state ended up illustrated on OVO, an e-wallet that can never enter it. Drawing them is the prerequisite for the below-minimum state and for #19's remainder case.
- ⚠️ **Production has a no-nominal-selected state the mocks never show:** all prices `Rp 0`, VA + CS dimmed, TERMURAH still on ShopeePay.
- ⚠️ **Four production details missing from BOTH poin payment sets:** QRIS as its own accordion group · six e-wallets incl. `Doku e-Wallet` and the full `ShopeePay/SPayLater` label · collapsed groups previewing logos (`+7 lainnya`) · step numbered **③ `Pilih metode bayar`**, not ② `Pilih pembayaran`. Fix `632:12570` first, then re-widen the desktop set.
- ❓ **What governs group expansion** — two production screenshots disagree (collapsed-with-chips vs all-expanded) at the same `Rp 0` state.
- 🚨 **The law's red is not the library's red:** the colour law names `#FF526F` for destructive; the library's `Error` is `Error`. Red is unused in the poin system so nothing is blocked, but one of them has to give. No style was shipped for it.
- ℹ️ **Pre-existing library drift, not ours:** a second remote style `RRQ/Surface/Surface-2` `Surface 2` is reachable in the 2.5 file — a duplicate of `Surface 2` from another library. Worth raising with the library owner while the palette is on the table.
- ⚠️ **`Rewards/Near Expiry` is a third warm token** next to `Primary` and `Error`; the existing rationale only argues it against gold.
- ⚠️ **Owed by Tech:** is method availability recomputed against `balance_after_redeem` server-side? The minimum is clearly already exposed (production renders it), but the client must not hardcode it.
- **Still open from the desktop gap audit:** coupon banners `641:12570` → desktop step-4 · wallet states empty/debt/hangus/near-expiry have **one** desktop mock between them (`972:9896`) · **no 1440 `Pesanan Selesai` at all** (money-only, coins-only, partial) · login + re-auth are 375 sheets needing the now-established centred-modal treatment · `Pembayaran Diterima` `1184:25559` · home tile badge at desktop tile size.
- **S7 journey card `1353:19821` still shows the old Lane B** (toggle ON → DANA disabled) and needs re-shooting on a remainder-below-minimum example.

---

**▶ WHERE THINGS STAND — status snapshot v5 (2026-07-29).** Three things are true that weren't on 07-22:

1. **Phase 0 is CLOSED.** Last gate — the publisher-compliance copy rule — is locked → **[publisher-compliance-copy-rule.md](publisher-compliance-copy-rule.md)**. Rule + banned/allowed lists + a live-site audit naming **11 failing strings by i18n key** + a CI lint spec. Unblocks **#1 "kill cheapest"**.
2. **Hero motion is real code**, not a spec — 5 standalone files in this repo root, zero deps, desktop + mobile, owner-tuned and verified. FE handoff → **[rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md)**. Supersedes the 07-27 artifact spec (wrong in 3 ways). **Not integrated — there is no application code in this directory.**
3. **PRIME landing (#16) is design-complete**, both breakpoints, plus a new desktop Panduan Poin modal. Diff vs live → **[prime-landing-figma-vs-prod.md](prime-landing-figma-vs-prod.md)**.
4. **E2 expiring-soon email is CLOSED** (2026-07-31) — design, production HTML, and a transparent animated hero, all in this repo root. **First shippable artefact in the whole project that a dev can use without Figma.** See Phase 3.
5. **THE REDEEM PATH IS NOW DESIGNED END TO END AT CHECKOUT** (2026-08-03) — the two things that blocked it are both built. **(a)** The confirmation drawer set `587:12570` is auto-layout and carries the redeem state as a second variant property **`pakaiPoin`** — **9 variants**, correct totals in both states (the boolean the 07-22 note called for was rejected: a Figma boolean can't invert, so one `Total Bayar` node would have shipped a visible 2.250 arithmetic error in the OFF state). **(b)** ~~The **DANA × poin-ON reversal** is fixed — DANA is disabled with the reason "Nggak bisa pakai poin" whenever poin are ON~~ 🚨 **RETRACTED 2026-08-20: that rule never existed.** The owner confirms there is **no payment-method restriction on redeeming**; redeem eligibility is a **per-product/game backend flag**. The real mechanism is a **method minimum tested against the remainder after poin** — carried by the 11 bank VA rows and the 2 Convenience Store rows, none of which the mock draws. See the 08-20 block below. Detail in the two blocks below (#6 item d, and the redeem-drawer section). **Still open downstream of these:** coins-only Rp 0 success page (#27) · order-status has no redeem line · #19 error states · E4 receipt email.

**▶ NEWEST DECISION (owner, 2026-07-31): GLOBAL NAV IS LOCKED, AND ALL FOUR CHROME COMPONENTS ARE BUILT.** Rewards leaves the header and becomes the **centre bottom tab** (Beranda · Pesanan · **Rewards** · Promo · Login/Akun); Chat moves into the sandwich; the **PRIME header button becomes state-conditional — shown ONLY to logged-in non-PRIME members**, absent for guests and PRIME. Built on page "RRQSHOP Rewards": **`Header / M`** `1014:21813` · **`Header / D`** `1020:22750` · **`Bottom Bar / M`** `1026:22496` · **`Sandwich Menu / M`** `1036:22248`. Also rebranded: the RRQ Topup logo is replaced by **`Logo / RRQSHOP`** `1017:21865` in every header. Full matrix, component table and 8 build consequences → **the 🔒 GLOBAL NAV DECISION block in Phase 2, above C2.**

  **Two things need someone other than design:** (1) 🚨 **guests have NO in-app route to PRIME** — the decision put it in the sandwich, the specified menu contents don't include it (consequence 2b); (2) 🚧 the desktop nav tab set is a **published-library component and is read-only**, so the Rewards destination could not be added there — the library owner has to do it (consequence 2). **C1** is probably answered by the sandwich poin block, but needs an explicit kill (consequence 3). Motion handoff is **no longer orphaned** — re-targeted to the nav tab on both breakpoints, consequence 6.

  **⏸ PARKED for later discussion (owner, 2026-07-31): purple Active state on the Rewards tab.** Idea: the Rewards tab's Active caret + label go purple instead of the system gold, since purple = poin by the colour law and a gold label under a purple token is a collision. **My recommendation was to keep gold** — active colour is one signal shared across all five tabs, and tinting one means "active" no longer has a single meaning; the purple token already carries Rewards identity in every state without spending that signal. Counter-argument that makes it a real debate: gold is *already* overloaded in this system (PRIME + primary CTA + selected). If it does go purple: use lilac `Rewards 4` not `Rewards 3` (the 11px label at `Rewards 3` on `Surface 1` is ~5:1, barely AA; lilac is ~2× that, gold ~8:1), and note that **desktop can't follow** — Active/Inactive live in the read-only library set `99:9986`, so you'd ship purple-active mobile against gold-active desktop until the library owner acts. It also adds a motion edge: purple Active label next to the lilac reduced-motion "new" dot makes *new* and *selected* compete in the same 24px.

**▶ THE ONE DECISION THAT NOW DRIVES WORK: PRIME's blanket discount is RETIRED** (owner, 07-29) — replaced by the poin multiplier. Scoped discounts (Flash Sale, coupons) survive. The design consequence is nil (the page is correct as built); **the non-design consequences are the critical path:**
  - **Material change to a live auto-renewing paid subscription** → subscriber notice before next renewal + PRIME T&C update + a grandfathering decision.
  - **Verify 3x poin actually beats the old discount rate.** Even at nominal parity it's a real-terms loss: poin expire in 60 days, are spendable only on RRQSHOP, and vanish if the user stops topping up. Do this before the notice goes out.
  - Kills `saved_price_product` ("Diskon PRIME") and makes compliance items **F7/F8 false, not just non-compliant**.

⚠️ **BLOCKED ON THE OWNER:** resize Figma node `870:20218` 368→445 · export an updated `Token.png` (asked 3×, still byte-identical) · **strict-vs-standard on the word `murah`** (rule §4 — default standard: keep `murah`, kill `termurah`; strict drops price adjectives from titles entirely = a real organic-traffic bet) · **someone with the actual RRQ reseller agreements to confirm the rule** (copy discipline, not a compliance sign-off, until then) · the three PRIME items above.

**▶ NEXT UP, runners-up already weighed:** ~~**#14 desktop poin page content bugs**~~ — **ALL THREE CLOSED, verified against `972:9896` on 2026-07-31.** Balance card `972:10742` reads **12.000** against "Setara dengan Rp 12.000" and a sidebar cell of 12.000 — 1:1 peg consistent. Both expiry strings (`972:10655`, `972:10780`) read **"kedaluwarsa"**, the correct KBBI spelling. The PRIME-bonus row tile `972:10621` is **`Rewards Surface 6`** (warm gold-tint); the cool `#262630` sits on the *expiry* row, which is correct per the muted-tile rule. Fixed by another session — this entry was stale. Remaining on the page is layer-name hygiene only: 8 text layers still named `Game Name` (`972:10780`, `972:10782`, `972:10794`, `972:10667`, `972:10666`, `972:12938`, `972:12947`, `972:10741`) and `972:12896` "Khusus PRIME " has a trailing space · **#17 post-cancel state** — management page (`967:19481`) and confirm dialog (`967:19975`) are both **DONE and settled**; what's left is (i) ~~the post-cancel state on the page~~ **BUILT 2026-07-31 → `PRIME Status Block / M` `1089:12588`** (page "2.5", x9614 y6300), variant `state = aktif | dibatalkan`. `aktif` = "Perpanjang otomatis:" / date / ghost pill "Berhenti berlangganan". `dibatalkan` = "Aktif sampai:" + muted "· Tidak diperpanjang" / date / **gold** pill "Berlangganan lagi". The pill flips ghost→gold on purpose: once cancelled, re-subscribing is the primary action and a destructive-looking ghost is the wrong affordance. Both variants are 62h so the page doesn't shift on state change. Dev swaps on `subscription.cancelAtPeriodEnd`; after the end date passes the block is replaced by the normal PRIME upsell row. ~~Still owed: the same treatment on the #13 account row~~ **BUILT 2026-07-31 → `PRIME Account Row` `1097:22032`** (page "RRQSHOP Rewards", x19623 y3544), variant `state = aktif | dibatalkan`, built from the owner's strip `1092:21682`, buttons owner-refined. `aktif` = green "Aktif" · "Perpanjang otomatis 8 Jan 2026" · secondary button **"Riwayat"** (`#2B2B2B` fill, `#5A5A5A` stroke). ⚠️ **Copy question left open:** that button's destination is the *management* page — cancelling happens there, not just history — so a user wanting to cancel is unlikely to tap "Riwayat". Same problem this doc already flags about the page's own name ("PRIME History undersells it — it's the management page"). Rename the page and this label should move with it. `dibatalkan` = grey **"Langganan Dibatalkan"** (owner's wording) · "Aktif sampai 8 Jan 2026" · gold "Lanjut Berlangganan". **Two decisions worth keeping:** (a) the cancelled chip is grey, not green — a green "Aktif" on a lapsing subscription is the same lie the management page told before this fix; grey follows the ledger convention (green earn / red spend / grey expired), and hierarchy comes from size not colour. (b) the active CTA is **"Kelola" → routes to the management page**, not an inline "Berhenti berlangganan": one cancel entry point instead of two, no destructive action inline on the account card, and the full label collided with the date string at 343 wide. The cancelled state keeps its action inline because "Lanjut Berlangganan" is constructive and needs no confirm step. Status-block tag realigned to "· Langganan dibatalkan" so both surfaces use one string, (ii) ~~row status variants (Gagal/Pending)~~ **BUILT 2026-07-31 → `PRIME History Row / M` `1112:12586`** (page "2.5", x9614 y3800), variant `status = sukses | pending | gagal`. Every row in the mock previously read "Sukses" in plain `Surface 8` with no visual encoding at all. Now `sukses` `Success` (same green as the "Aktif" chip), `pending` `Surface 7` (muted, matches expired/pending), `gagal` **`#FF526F`** — the file's established danger red, 33 existing uses. ⚠️ The one stray "Gagal" label elsewhere in the file is `Surface 7` and should be brought onto `#FF526F`. Only the status token changes between variants: date, period, payment method and amount stay, because a failed charge is still a real attempt against that period. 🚨 **GAGAL IS A DEAD END — new open item.** A failed renewal is exactly when a subscriber must act, and the row offers nothing: no retry, no "perbarui metode pembayaran", no statement that PRIME lapses on the period-end date. Same shape of bug as the cancel dialog that confirmed an action leading nowhere. Decide: inline retry on the row, a banner above the list, or a push/email deep-linking to payment, (iii) ~~placeholder data bugs~~ **FIXED 2026-07-31 across all 10 rows of `967:19481`**: `31/2/2025` (31 February) and the ambiguous `1/3/2025` D/M format replaced with site-format dates (`8 Des 2025`, monthly sequence ending `8 Jan 2026` so it lines up with the status block); `Rp 20,000` → **`Rp 25.000`** (real price, period separator per site convention). ⚠️ The date boxes were sized for the old `1/3/2025` string, so the longer dates wrapped to two lines and collided with the period row — all 10 set to hug (`WIDTH_AND_HEIGHT`), verified single-line with no collision against the amount column, (iv) ~~`Game Name` / `Pilih pembayaran` layer names~~ **DONE** — renamed by content (Tanggal / Periode / Nominal / Status / Judul Panel / Label Aksi); zero `Game Name` layers remain on this frame. Also deleted **10 hidden leftover `GoPay` text nodes**, one per row · **#14 wallet** (desktop layout + near-expiry ≤2wk amber — closes the expiry-clarity risk) · **#20 T&C page** (4 inputs queued: product-group rate table, DANA exclusion, reseller/pricing statement, PRIME benefit terms incl. grandfathering) · **#1 compliance remediation** (kill list is concrete: `benefits_best_price` + desc, `app.title`, the `resmi`+`harga murah` half of `faq_answer_1`) · the F7/F8/`saved_price_product` sweep.

**▶ SMALL + KNOWN, not blocking:** PRIME landing FAQ chevrons sit ~20px below centre on every row, both breakpoints (targets in #16) · mobile card-1 frame still named `Selalu ada diskon` (= the F11 string, so a grep to confirm removal gets a false hit) · headlines + section titles on both PRIME breakpoints still named `Game Name`.

**Earlier framing — RRQSHOP Rewards LANDING PAGE.** (Program landing/intro page for RRQSHOP Rewards — clarify w/ owner: marketing/intro landing vs event landing #22.) Build on page "RRQSHOP Rewards", clone real panel chrome, follow color/grammar laws below. Reuse built assets: glyph, strip 3-state, nominal card set, homepage tile badge, wallet Informasi Poin, Panduan Poin drawer.

**Landing page — IN PROGRESS (2026-07-23):** Scope locked w/ owner = **evergreen marketing/intro** (NOT event #22), **mobile+desktop**, **guest-first → Masuk/Daftar CTA** (member/PRIME = lighter variants). Canvas: all RRQSHOP work is on single page 0:1 "2.5" (x≈9000+ region); new landing frames at x=13800.
  - ✅ **Mobile guest landing BUILT** (`861:12570`, "Rewards Landing - Guest - M", 375×1380). Chrome cloned (status 725:17038 / top nav 725:16964 / home 725:17037). Sections (vertical AL content col 861:12571 @ y106): **Hero banner** (866:12595) + Cara Kerja (3 numbered steps) + Berapa Poin (prose) + PRIME gold card (gold border, "3x" gold, Pelajari PRIME ›) + Masa Berlaku (prose) + Final CTA band.
  - **Hero = Discord-Quest-style cinematic banner** (owner ref: Discord "Introducing Orbs", discordquest.com). Rounded banner, purple/violet **nebula bg + blurred bokeh**, **glowing hero token** (RRQ glyph clone @132px + drop-shadow glow) + **floating satellite glyphs + white sparkles**, bottom scrim, eyebrow + Barlow-Black headline + subcopy + **gold "Masuk / Daftar" + ghost "Cara kerjanya"** buttons. Stays on color-law: violet only (no gold/teal in art; gold reserved for PRIME/CTA). Replaced the earlier flat/informational hero (owner: top must entice, not just inform).
  - **HERO MOTION (owner ref: Discord Nitro intro, 2026-07-24):** live interactive artifact = source of truth (Figma stays static per motion law). URL: https://claude.ai/code/artifact/319c82da-891e-4368-8b80-07d2edcb9140. Two effects on the hero banner: (1) **inward light-rails** — `<canvas>` starfield of ~95 radial streaks accelerating toward a vanishing point behind the token (owner picked INWARD over outward; owner: stars must be **line-shaped light-rails**, not dots; length/brightness scale with radius, fade near center). (2) **token shine-sweep** — Nitro-style light band swept across the RRQ glyph every ~4s, **alpha-masked to the glyph silhouette** (`mask-image`), + idle float + glow pulse. Reduced-motion → static (rails become faint dots, shine off). Build = CSS keyframes + ~60-line canvas (NOT Lottie/video). Glyph asset = 480px transparent PNG exported from Figma (694:17559), grey canvas bg keyed out via PIL. Barlow ttf faces inlined as data URIs (CSP blocks font CDNs); body = system stack (no local Inter).
  - hero-motion FE handoff written → [rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md) (canvas light-rails + shine CSS + reduced-motion + QA).

**⚠️ DIRECTION CHANGE (2026-07-24): OWNER BUILT DESKTOP LANDING = canonical design.** Owner's desktop `870:19298` ("Landing Page", 1440x2560) supersedes my exploratory guest hero. New structure (NOT my earlier Cara Kerja/Berapa Poin/PRIME-card version): Header → **hero** (gradient `#D9A6FC→#AB5CEB→#500EC1→#191716` + floating 3D art [chest/gift/crown/coin/key] + big Rewards Token + glow; headline **Barlow Black Italic** "Top up hemat dengan **RRQSHOP Rewards**" [gold on RRQSHOP Rewards]; sub; gold **Login**) → **3 feature cards** (Poin bisa digunakan untuk bayar / Dapat poin setelah pesanan selesai / 1 poin setara dengan 1 Rupiah) → **"Cara mendapatkan poin"** panel (lilac `Rewards 4` Barlow-Black title; 01/02/03 purple token-number badges; steps: login-wajib / belanja badge Rewards / poin masuk saat selesai) → **"Menggunakan poin"** panel (+ payment-toggle mock, component `631:12572`) → **"Sering Ditanyakan"** FAQ (9 PRIME-subscription Qs) → hazard marquee + footer. Panels `Rewards Surface 1`, cards `Surface 1`.
  - ✅ **MOBILE built to match owner desktop** (`887:12619`, "Rewards Landing - M (owner design)", 375×2950, on page **"RRQSHOP Rewards"** `362:12714` at x=18012 y=6100 — beside the owner desktop `870:19298`). NOTE: owner desktop landing lives on page "RRQSHOP Rewards", not "2.5". Reused owner atoms: 3D hero art (scaled via SCALE constraints), Rewards Token, feature-card badges, 01/02/03 step badges, payment mock instance, FAQ chevrons, **Footer / M** (`1:2568`, includes marquee), mobile top nav. Guest-first (Login CTA). Fonts inlined for reference artifacts.
  - **Earlier exploratory guest mobile `861:12570` (light-rail token hero) = SUPERSEDED** by owner design; safe to delete. Hero MOTION (light-rails + token shine, artifact 319c82da + handoff) is **parked** — owner's real hero uses 3D-gift art + glow, not the token-shine hero; light-rails could still layer behind it later if wanted (optional).
  - 🗄️ **HERO MOTION SPEC'D (2026-07-27) — SUPERSEDED 2026-07-29 by the real build below.** Kept for history; do NOT build from this. Three things in it are now wrong: stars go **INWARD** not outward, count is **79** not ~160, and the props float from a **JS engine** not CSS keyframes. The artifact (https://claude.ai/code/artifact/1029b7a4-9993-44a6-9f44-299e6b598614) is a dead end — the real source is the files listed below, and the handoff is [rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md) (written 2026-07-29; the link was dangling until then).
  - ✅ **FAQ panel made responsive (desktop `883:6642`, 2026-07-27):** entries were absolute-positioned (fixed 335 bg rects, fixed text width, chevron pinned by x) → converted to real auto-layout at **548w**: text FILL/wraps, chevron pinned right, bg rects fill+follow, container vertical AL (8px gap), multi-line Qs auto-grow. NOTE: `FAQ / D / Closed` master (`858:17954`) is a **read-only library component** → rows had to be **detached** to become resizable. Gradient/pattern gloss preserved (PATTERN paints can't be rewritten via API — geometry/mirror only; owner hand-fixed final gradient position).
  - ✅✅ **HERO MOTION BUILT FOR REAL (2026-07-29).** No longer a spec or an artifact — five standalone files in the repo root, zero deps, zero build step. **These are the source of truth.**

    | File | Role |
    |---|---|
    | `rewards-hero-warp.js` | ray-field engine, ES module, exports `initWarp` + `PRESET` + `PRESET_MOBILE` |
    | `rewards-hero-icons.js` | floating-props engine, exports `initIcons` + `PRESET` |
    | `rewards-hero-warp.css` | layer CSS (canvas, dome mask, token cut, icon layer) |
    | `rewards-hero-copy.css` | title / subtitle / CTA, desktop + mobile |
    | `rewards-hero-warp.html` | preview harness: 7 layer toggles, 3 slider panels, drag-to-place |

    **FE HANDOFF DOC → [rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md)** (2026-07-29). Give this to the front-end dev: integration snippet, 5 host requirements, both presets, responsive contract, a11y, perf numbers, browser support, 7 traps that each cost real time, the 6 deliberate deviations from Figma, and a 14-item QA checklist.

    Preview: serve the folder and open `rewards-hero-warp.html` (`?tune=1` for sliders). **Must be HTTP** — `file://` blocks the ES module import as cross-origin.
    ```bash
    cd "/Users/serling/RRQ Topup" && python3 -m http.server 8080 --bind 127.0.0.1
    ```

    **7-layer stack** (back→front): Background.png · ray canvas · Token.png · 4 CSS side-glow lobes · Pattern.png · 6 floating icons · copy. Each has its own toggle in the harness.

    **Band height RESOLVED = 445, not 368.** Node `870:20218` needs resizing in Figma (still owner TODO). Two measured reasons: Token.png is bottom-anchored (alpha runs to art row 893 of 894), so cropping to 368 deletes 10.4% of the token off the bottom; and the vanishing point is band-relative (`cy = H*vpy`), so at 368 it lands 74 art px higher — rays would converge *inside* the mark. Frame `870:20216` is 1446×447 = exactly half the 2892×894 art, so the aspect was already right.

    **Star warp — INWARD, owner-tuned, do not "fix" these.** 79 rays, `bow 0.08 / bowEase 2.55`, polar path. A chord-path model (bend as shape, 18% of chord vs 1.1%) was built 07-29 and **rolled back on the owner's call** — still reachable via `arcMode: 'chord'` + a toolbar toggle. `bow` is signed (negative = opposite arc); slider was clamped to 0 and is now −0.7…0.7. Verified: mirror symmetry 0px across 25 pairs, monotonic inward, 60fps.

    **Icons — exact Figma placements, not estimates.** Desktop from `870:20216`, mobile from `883:19494`. Depth comes from the artwork: `key`/`coin` have maxAlpha 151/178 (drawn faded) so they float slower + shorter; the four solid ones get the larger throw. Float measured off the Nitro clip: periods 4.6–6.8s, non-harmonic so they never re-align, ±3% of band height, rotation ±3.5°. **Icons punch themselves out of the ray canvas every frame** via a `cutout` hook — a static CSS mask can't track a moving shape, and `destination-out` needed per-icon alpha normalisation because a translucent sprite only *dims* rays otherwise.

    **Copy layer — 4 corrections to the CSS the owner pasted:** button had no background (real fill `Primary`), was missing one of two inset highlights, had no label typography (Barlow Black 16, not Inter), and `left: 648px` was a 1440-frame assumption (it's centred in the real 1446 frame). Visible label is **"Login Sekarang"** (the layer is *named* "Pilih pembayaran" and holds a hidden "Lanjut Bayar").

    **Title stroke + inner shadow = ONE SVG filter** (`#hero-title-inner-shadow`, inline in the HTML — CSS can't do inner shadow on text, and Chrome won't resolve a filter from an external file or a data: URI). ⚠️ **Figma's MCP silently drops text strokes, gradient paints and effects** — `get_design_context` returned a bare `<p>` for all three, which cost 3 rounds of inferring from pixel renders. For any remaining hero work, read paints off the file directly. Current values, **all four now deviate from Figma on the owner's call**: stroke gradient `#D9A6FC → #4C1894` at **60%** (Figma: `#FFFFFF → #3513AF` at 100%), diagonal 12.7° driven off the measured text ink; inner shadow `#DFB1FF` (measured `Rewards 4`), dx 3 / dy 4 / blur 2.5. Filter primitives scale off **title font-size**, not band width — those diverge on mobile (0.59 vs 0.26).

    **Mobile DONE (2026-07-29).** Hero `883:19494` = 375×290, aspect 1.29 vs desktop 3.23 — it's the same art **centre-cropped**, so **no new assets**: Figma places the bg at 951px wide and `object-fit: cover` on a 375×290 band renders 938×290, within 1.4%. Breakpoint **767px on band width** (`@container`, so a hero in a narrow column gets it too). Copy goes **in flow** on mobile so the band can grow — absolutely positioned children contribute nothing to parent height, so the desktop approach overflowed at 320. Type does **not** scale uniformly (title 54→32, sub 16→14, CTA label 16→16), so mobile is restated, not multiplied. Verified vs frame: title y52 h76 w343, sub y140, CTA y220 h36 w144, fonts 32/14/16 — all within 1px. Tested 320 (band grows 247→252, nothing clips) and 430 (title hits its clamp). Ray field has its own `PRESET_MOBILE` (count 44, fan 0.95) — **starting values, not measured; no mobile reference clip exists.**

    **Page background must be `Surface 1`** and the hero's own background must match it. The art *dissolves* at the bottom (mean alpha 21.7 on the last row), so the hero background is part of what you see there. Was `#0b0810` → a visible 13/255 band at the seam; now both read one `--page-bg` var → 2/255. Deleting that "redundant" declaration puts the band straight back.

  - **STILL OPEN (hero):** owner to resize Figma node `870:20218` 368→445 · **owner still owes an updated `Token.png`** (asked 3×, md5 unchanged `f452bfab…`) · dial `PRESET_MOBILE` on a phone-width window (values are reasoned, not measured) · verify time-based timing on a 120Hz screen (toolbar shows `fps · field clock /s`; want ~0.96/s at 120fps) · `Pattern.png` will moiré on 1x displays (16px pitch downscaled 2x) · `Background.png` → WebP/AVIF (256KB, but it's the live mask source so verify alpha survives) · ~~write the FE handoff doc~~ ✅ 2026-07-29 · lift `.layer`/`.glow*`/`@keyframes sideGlow` out of the harness into shipped CSS · self-host Barlow + Inter · member + PRIME variants (deferred) · FE integration (no app code in this dir; target framework never established).
  - Copy applied from Panduan Poin drawer content (cara kerja steps, berapa poin prose, 60-hari, PRIME 3x). Reused gold pill fill (solid `Primary`) from Tutup button 725:17025.

**Last session recap (2026-07-22):**
- ✅ **#15 Transaction history DONE** — auto-layout component sets: mobile `Pesanan Card / Mobile (AL)` (`825:17404`) + desktop `Pesanan Card / Desktop (AL)` (`829:17404`), props `Status`(5)/`Pakai Poin`(bool)/`Beli Lagi`(bool); swapped into mobile (767:17575) + desktop (767:4297) layouts. Old absolute set deleted.
- ✅ **Panduan Poin drawer** (`725:16668`) + **wallet Informasi Poin** panel (exp `732:16577` / collapsed `729:18311`) + wallet restructure (auto-layout wrapper, +purple ledger signs, empty/debt states).
- ✅ **Rewards header button motion** — spec + interactive artifact + FE handoff doc [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md).
- 🔶 **Drawer redeem (Pakai Poin)** — sample fully-AL + prop `839:12570` DONE (reference); applying to drawer SET `587:12570` (5 variants) still OPEN.
- **PM+Backend feedback applied** (rate model reframe, E2 single/E3 dropped → 5 emails, coupon unblocked, Rule 1.10b).

**Figma file:** `RWkIM1Q8o5pqt6eK3O8WBe` (RRQ-Topup-2.5) · main work page: **"RRQSHOP Rewards"** (some component sets on page "RRQ Topup 2.5" / "2.5").
**Before editing Figma:** load skill `figma-use`. To match site style accurately, **clone real panels for chrome** (dark `Surface 4` bg, corner-rivet dots, hazard-stripe Barlow-Bold titles) — don't rebuild from scratch. Fonts: Barlow (Black/Bold/SemiBold/Medium) for headings/numbers, Inter for body. Colors: `Surface 5` page, `Surface 4` panel, `Surface 8` text, gold `Primary` (PRIME), purple `Rewards 2` (poin).

**Balance rule (locked 2026-07-31):** every headline balance = **`balance.available` only**, never `available + pending`. Pending lives in the ledger. Protects the 1:1 peg — see #28.

**Color/grammar laws (locked):** purple = poin · gold = PRIME · green = temporary boost (event OR new-member bonus) · red+(−) = spend/expired. Multiplier copy "2x/3x" everywhere; percentages ONLY in T&C/tier-table. All numbers from `/preview` (never client-math). Unit word = "poin" (ID); i18n "points" for MY/PH.

**DONE ✅ (Figma built):**
| Surface | Node / set |
|---|---|
| Nominal card set (24 var) | `529:895` |
| Homepage tile badge (3 state) | `556:1271-1273` |
| Checkout drawer set (5 var) | `587:12570` (+ref `583:12570`) |
| Payment selection set (5 var) | `632:12570` |
| Voucher set (3 var) + PRIME popup | `641:12570` · popup `629:15489` |
| Order status mobile (sticky poin banner, 2 copy states) + desktop (left-col card `657:15144`) | order frames on RRQSHOP page |
| Account page (regular + PRIME) | owner-built |
| Wallet page main + empty + debt + PRIME-bonus row (restructured 2026-07-22: 3 sections in auto-layout wrapper 729:18298, all 351w; ledger sign convention reversed) | `681:16812` · empty `706:12570` · debt `708:12570` |
| Panduan Poin drawer (#2/#18) — mobile bottom-sheet, 5 concise sections | `725:16668` (RRQSHOP Rewards page; cloned from "Panduan Berbelanja" drawer 724:17668) |
| Wallet "Informasi Poin" TL;DR panel (GoPay-Coins style) — 2 states: expanded / collapsed | expanded `732:16577` (main wallet) · collapsed `729:18311` (empty-state 706:12570) — 6 icon-rows + "Info lebih lanjut seputar Poin" pill |
| Glyph components | Default/Small `507:1468` · Multiply(event) `507:1467` · PRIME(gold) `554:1663` · Switch(purple) `596:16875` |

**REMAINING (V1):** ~~how-it-works page~~ ✅ DONE as **Panduan Poin drawer** (`725:16668`) · event landing (#22) · coupon pages (#24 My Coupons, C6 card — **UNBLOCKED 2026-07-21**, backend adding cashback write-path) · **5 email templates** (E1,E2,E4,E6,E7 — E3 expired dropped; E2 = single reminder, key retention lever) · wallet desktop layout · near-expiry copy variant (≤2wk amber). **Login/register #11/#12 = V2 (skip).** **Handoff package = to assemble.**

**PM+Backend feedback applied (2026-07-21, `RRQ Rewards — Feedback for DESIGN.pdf`):** rate model reconciled to backend "Model T" (base = flat % per game/product-group, NOT rupiah-amount tiers; events replace; PRIME configurable per-game; single max-rate ceiling ~4%) — **no Figma rework, built UI valid**. Doc contradiction resolved (see reconciled RATE MODEL + LOCKED stacking blocks = single source). E2 single-reminder, E3 dropped, coupon unblocked, Rule 1.10b (suppress new-member for already-PRIME), working agreement (Design=UI/UX auth, Tech=mechanics auth).

**Panduan Poin — FINAL as DRAWER (owner pivot 2026-07-22):** full-page version built then SCRAPPED. Owner: mobile = bottom-sheet drawer, content concise/on-point (our users bounce on info overload). Cloned existing "Panduan Berbelanja" drawer (724:17668) chrome — dimmed game-page backdrop + dark sheet + hazard-stripe section headers (Barlow Bold, uppercase) + gold "Tutup" pill + grabber. Title "Panduan Poin". **5 sections (Inter body, reflow auto-layout):**
  1. CARA KERJA — ordered list: (1) top up game favoritmu (sebagian besar dapat poin, ada badge poin) (2) poin masuk saat pesanan Terkirim, bukan saat bayar (3) pakai poin potong harga transaksi berikutnya.
  2. BERAPA POIN YANG DIDAPAT — **owner copy, NO table**: "Poin dihitung dari persentase harga nominal layanan yang dipilih. Rate poin bisa berbeda-beda tergantung game dan nominal yang dipilih." (tier-rate table DROPPED here → lives in full T&C #20).
  3. MEMBER PRIME — "dapat 3x poin tiap transaksi, tanpa nunggu event."
  4. MASA BERLAKU POIN — "60 hari, diperpanjang otomatis tiap top up."
  5. PAKAI POIN — **owner copy**: "Nyalakan toggle 'Gunakan Poin' di bagian pembayaran ketika berbelanja."
  **Owner edits applied:** (1) poin-calc = prose not table; (2) removed top-right (i) button (redundant — already inside info); (3) pakai-poin = single toggle line.
  **Dropped for concision (flag):** ~~DANA-no-redeem exclusion NOT in drawer~~ **retracted 2026-08-20** — what surfaces at checkout is **product-level non-eligibility** (`non-redeemable`, `610:12570`), a backend flag; no payment-method exclusion exists. 8-bullet S&K + tier table moved to full T&C page #20 (still to build).
  **Entry point:** wallet "Panduan" pill (683:17507) + ⓘ. Dev: PRIME section can deep-link PRIME page (optional).

**Wallet page restructured (owner, 2026-07-22):** body sections now wrapped in ONE VERTICAL auto-layout **Frame 116034** (`729:18298`), 351w @x12, itemSpacing 12, order = SALDO card (694:17538) → Informasi Poin (732:16577) → Riwayat (683:17326). All three now 351 wide, edges aligned (Riwayat refit from full-bleed 375 → 351). Page 375x1534, bottom bar (702:16380) y1438.
  - **⚠️ LEDGER SIGN CONVENTION REVERSED (supersedes earlier "no +/settled-only" decisions in #14):** earn now shows **"+" prefix, purple `Rewards 4`, Barlow SemiBold 15** (e.g. "+10.000", "+240"). Spend = **"−" muted gray `Surface 7`** ("−500"). Expired = **"−" very muted `Surface 6`** ("−120", more muted than spend ✓). Canceled = **"0" `Surface 6`**. **Pending NOW shown in ledger** with amount (purple) + "Pending • [date]" subtitle (was: pending hidden). Dates = `#8B8B96`. Row title = Inter Medium 14 `Surface 8`. "Tampilkan lebih banyak" footer.

**Wallet "Informasi Poin" TL;DR panel (2026-07-22, GoPay-Coins pattern per owner ref):** `732:16577` (internal frame name still "Sekilas tentang Poin"), in wrapper Frame 116034 between SALDO & Riwayat.
  - **Two states (collapsible):** EXPANDED `732:16577` (title + up-chevron + 6 icon-rows + footer pill) = main wallet default · COLLAPSED `729:18311` (351x52, title "Informasi Poin" + down-chevron only, rows hidden) = used in EMPTY-STATE wallet (706:12570). Empty state also uses an auto-layout body wrapper (Frame 116036 `740:18368`). Rationale: empty wallet has no history to scroll, so guide starts collapsed to keep it tidy; populated wallet starts expanded. Dark panel + rivets chrome, collapse chevron, 6 icon-rows (distinct per-row glyphs — owner refined from uniform), footer pill "Info lebih lanjut seputar Poin" + green "?". Rows adapt locked poin rules: (1) pakai poin buat hemat (2) bisa dipakai hampir semua game (3) poin dipakai lebih dulu, sisanya gabung metode lain (4) 1 poin = Rp1 (5) nggak bisa diuangkan/transfer (6) berlaku 60 hari, diperpanjang otomatis. Owner retitled "Sekilas tentang Poin" → "Informasi Poin". Dev: footer "?" pill → opens Panduan Poin drawer (725:16668). Wallet re-laid-out (page 375x1534, Riwayat shifted to y819, bottom bar y1438).

**Backend flags — ALL CONFIRMED 2026-07-21 (PM+Backend):** `/preview` per-source breakdown + new-member potential + eligibility ✅ · `promo_code.cashback_rate` write path ✅ (being added) · expiry-reminder built (single reminder, CMS `days_before`) ✅ · CMS display-label field per event ("2x"/"1.5X") ✅ · single max cashback-rate ceiling enforced ✅.

---

## Program key facts — see HANDOFF.md §1 and §2

**Mechanics and the laws are NOT restated here any more (deduped 2026-08-20).** They live in one place so they cannot drift apart:
- **[HANDOFF.md §1 — Program mechanics](HANDOFF.md)** — rates and the full earn pipeline · events · PRIME rate · new-member override · eligibility gate · earn-on-delivery · net earn base · floor rounding · expiry (rolling wallet-level, 60d default, admin-tunable) · redeem rules · refund/clawback · every API field.
- **[HANDOFF.md §2 — The laws](HANDOFF.md)** — colour law (+ the Figma style names), copy law, compliance law, accessibility floor, motion law.
- **[HANDOFF.md §9 — Figma node index](HANDOFF.md)** — the authoritative node IDs.

**Three things this section used to get wrong, corrected against HANDOFF on 2026-08-20:** (1) it called the display name *"Points (RRQ Rewards)"* — the copy law is **`poin`** lowercase, program name **RRQSHOP Rewards**; (2) it said *"expiry 45–60 days"* — it is **60 days default, admin-tunable, rolling and wallet-level**; (3) it framed redeem eligibility as a DANA carve-out — it is a **per-product/game backend flag** (see the retraction in the 08-20 session log).

**Kept here because it is provenance, not spec — the rate-model reconciliation:** the amount-tier framing this doc used before 2026-07-21 was replaced by backend **"Model T"** (PM+Backend FINAL, 2026-07-21): base rate is a flat % per game/product-group with separate member and prime columns, **never tiered by rupiah amount**; events REPLACE the base; `2x` is a CMS display label over a real %. **Design consequence at the time: zero Figma rework** — the pills and labels were already driven by `/preview` + CMS label fields. The one downstream obligation is that **#20 T&C must present tiers in product-group % framing, never rupiah amount-bands**.

**Working agreement (unchanged):** Design authoritative for UI/UX · Tech (`cashback-rewards.md`) authoritative for backend mechanics · any conflict → Tech↔Design sync, no silent override.

---

## Phase 0 — Blockers (do before mockups)

### Research workstreams
- [x] R1. Program name → **REWARDS** ✅ (owner decision). Program = "RRQ Rewards".
  - [x] Unit → **no separate word; glyph + number** ("◈ 500") ✅. Balance/prices shown as glyph+figure, not "500 Rewards".
- [x] R2. Rewards glyph → **DECIDED** ✅: rounded-diamond (squircle) token, **royal purple** (contrast vs RRQ yellow-orange, avoids ML-Diamond blue), chamfered facets (light top / dark bottom), thick black outline, black **RRQ "R" slash monogram** center.
  - **Size-adaptive ratio rule:** small sizes use a bigger monogram-to-token ratio so the R stays legible on product cards; large/hero uses the fuller token with more facet detail. Two (or three) cuts: hero / medium / small.
  - Reference implementation: product card earn pill "◈ +35 Points" under price.
  - [x] Copy rule → **HYBRID (refined)** ✅: **compact surfaces** (product-card pills, header chip, balances, prices) = glyph + number only ("◈ +35", "◈ 1.250"). Word "Points" only at **explicit earn/education moments** (checkout earn line, order-success, wallet header, upsell copy). Never "Rewards" as countable noun.
  - [x] PRIME highlight → **GRID STRIP, implemented** ✅ (2026-07-10): cards stay clean ("◈ +35" only, no per-card PRIME element). One strip between step-1 header and first product section, **three audience states, no percentages — FINAL (implemented 2026-07-10)**:
    - **Guest**: "Top up dapat poin, belanja lebih hemat." + gold **Login** button.
    - **Member**: "Tiap top up dapat poin. PRIME dapat **3x** poin." + chevron → PRIME page.
    - **PRIME**: "[PRIME logo] dapat **3x** poin. Mantap!" — logo replaces the word (one PRIME representation, subject of the sentence), thin divider between poin-glyph and logo, no right-side element (no action = empty slot is correct).
    - **Unit word locked: "poin"** (Bahasa spelling, lowercase) on ALL ID surfaces — supersedes "Points". One form everywhere (checkout, wallet, email, T&C); MY/PH localize to "points" via i18n, never hardcoded. Multiplier framing ("3x") replaces %; pill numbers from /preview are the proof. PRIME pill strikethrough ("◈ +105 ~~+35~~") stays as the PRIME-member card treatment; loud PRIME chip only at checkout upsell.
  - Wallet name still TBD ("Stash" suggestion parked).
- ~~R3. Guest auto-account (escrow + OTP-claim)~~ → **DEFERRED to later phase** (owner call, 2026-07-02). V1: guests simply don't earn; only logged-in members earn Points. Research + Codashop blueprint preserved in [research doc](cashback-research-R1-R3.md) for when it's picked up.
  - Backend V1 support confirmed: public `GET /preview` + `potential_cashback` field on guest responses → the "sign in to earn ~X" nudge (mock #5) is backed by a real API.
  - Deferred with it: #10 guest order-tracking claim CTA · E5 guest "claim your points" email.
  - V1 consequence: guest checkout shows a **"Login/register to earn ◈ Points"** nudge instead of escrow (covered by #5 checkout + #11 login).

### Brand constraints (apply to all visual work)
- No gem / diamond (product clash) · glyph should stay visually separable from the RRQ gold-coin medallion logo · works in Bahasa/MY/PH.

### Rules to lock (design can't start without these)
- [x] Earn base = discounted / net amount ✅ decided
- [x] Rounding = **round down / floor** ✅ decided (fractional pts dropped, e.g. Rp 10.550 @ 1% = 105 pts)
- [x] Expiry — **LOCKED by backend** ✅: rolling **wallet-level** expiry, default 60d (admin-tunable). Whole balance shares ONE `expires_at`; every earn *activation* (delivery) resets it to +60d @ 23:59:59 WIB. Redemption never extends. API returns `expires_at` → UI shows one date, not per-lot dates.
- [x] **Publisher-compliance copy rule — LOCKED 2026-07-29** ✅ → [publisher-compliance-copy-rule.md](publisher-compliance-copy-rule.md). **Last Phase-0 gate; closing it unblocks #1 "kill cheapest".**
  - **The rule in one line:** never make a price claim whose comparison set is unbounded or includes the publisher's official channel. Superlatives are unbounded by construction, so superlatives always fail.
  - **BANNED:** `termurah` / `paling murah` / `harga terendah` · `harga terbaik` / `best price` · `lebih murah dari [anything unnamed]` · any reference to the official/in-game store's price · **`resmi` + a price claim in the same sentence** (each fine alone, together reads "official channel, and cheaper" — the exact implication we're avoiding) · price-match promises · `diskon` on a base game-currency price.
  - **ALLOWED:** `murah` as a plain adjective (absolute, ranks against nothing — keeps the category's main search intent) · `hemat` when the saving is named as ours ("hemat dengan RRQSHOP Rewards", "hemat sampai Rp 5.000") · non-price superlatives (`tercepat`/`terpercaya`/`terlengkap`) · absolute facts (1 poin = Rp 1, "3x poin") · `resmi` about RRQSHOP itself · customer reviews saying "Harga Terbaik" — UGC is exempt but **must not be promoted into first-party copy**.
  - **The reframe (why this gate blocked #1):** say what we give back, not what the price beats. cheapest → dapat poin tiap top up · best price → harga transparan + poin balik · PRIME cheaper → PRIME 3x poin. A return we give has no comparison set. This is Rewards' whole job, so compliance and the stronger retention story are the same edit.
  - **LIVE-SITE AUDIT DONE — 10 real failures, all keyed.** Copy lives in an **i18n message bundle**, so remediation is a locale-file edit and the rule is **lintable in CI** (the only version that survives the next copywriter). Priority = F1–F3, they're `<title>`/meta/og and indexed: game `seo_title` **"Top Up Mobile Legends Termurah | Banyak Promo Menarik!"** (also the visible H2 above the nominal grid) · game `seo_description` · `app.title` "Layanan Topup Termurah" · `benefits_best_price` **"Harga Terbaik"** + its desc · `faq_answer_1` (the `resmi`+`harga murah` sentence) · PRIME `suggest_title` "Dapatkan Harga Lebih Murah" / `suggests_desc` / `benefit_flashsale_desc` / `landing_benefit_desc`. **PASS, leave alone:** `footer_marquee` "Top Up Murah" (and it ships inside `Footer / M` `1:2568` on the Rewards landing), home `seo_title`, `Hemat`/`Hemat sampai`, T&C's `platform resmi RRQSHOP`.
  - **`TERMURAH` product tag fails** — and it renders from a **CMS image, not the message bundle**, so no lint or grep will catch it. Recommend **`HEMAT`** as a 4th `tag` value in nominal-card set `529:895` (same overhang slot, same 7-char budget; `PROMO` collides with the existing tag state).
  - **T&C gap:** the live T&C says nothing about pricing or channel relationship. Add to #20 — "RRQSHOP adalah penyedia layanan top up pihak ketiga. Harga ditentukan sendiri oleh RRQSHOP dan tidak mewakili harga resmi dari penerbit game." The one place saying it protects instead of markets.
  - **Nothing built has to be rebuilt** — checked: hero headline "Top up hemat dengan RRQSHOP Rewards" **passes** (`hemat` bounded by "dengan RRQSHOP Rewards"), guest strip "belanja lebih hemat" passes, all `2x`/`3x` multiplier copy passes and is *why* it passes.
  - ⚠️ **Open owner calls:** (1) **strict or standard on `murah`?** Default standard (keep `murah`, kill `termurah`) — strict means dropping price adjectives from titles entirely ("Top Up Mobile Legends — Aman, Cepat, Dapat Poin"), defensible only now that Rewards exists, but a real organic-traffic bet. Flagged, not decided. (2) `TERMURAH`→`HEMAT` tag re-export. (3) **Nobody here has read the actual RRQ reseller agreements** — this is copy discipline, not a compliance sign-off, until someone holding the contracts confirms it. (4) does the lint actually land in CI.
- [x] **Rate stacking — LOCKED by backend spec** ✅ (`cashback-rewards.md` + simulations; re-confirmed by PM+Backend 2026-07-21 — **THIS pipeline is the single source of truth**; the "RATE MODEL reconciled" block above is its plain-language POV, they agree):
  - Pipeline: `base rate (per game/product-group, member vs prime column) → game/product EVENT REPLACES base (per-tier %) → + payment-method event additive pp → + promo cashback_rate additive → floor → per-user event cap → global max-rate ceiling (config number, e.g. 4%) → debt netting → event budget`.
  - **PRIME:** per-game `prime_rate` (fallback: `prime_percentage` setting) — admin-set per game (a config value like 3%, not a hardcoded rule). Event boosting PRIME = operator choice per event → "flat, event-immune" achievable.
  - **PRIME + coupon = automatic downgrade to member rules** (prime rate dropped). Not a user choice — UI must WARN on coupon-apply for prime members, not offer a radio picker.
  - Coupon for regular member: never removes event; adds promo cashback_rate if it carries one. **Coupon cashback write-path CONFIRMED for V1 (2026-07-21)** — no longer backend-blocked.
  - **New-member first transaction: flat override % ignores everything** — needs its own UI badge/moment. **Rule 1.10b (2026-07-21): the new-member override / drawer `state=new-member` / guest dual-number nudge must NOT fire for users who were already PRIME before their first transaction** (already-PRIME gets normal PRIME, no flat new-member bonus). Dev: state-selection guard, not a new frame.
  - No rate row configured → game earns nothing (eligibility gate) — matches "not every game eligible".

---

## Phase 1 — Customer pages / surfaces (21)

### Reframe
- [~] 1. Homepage / landing — **tile badge system DONE** ✅ (3 states, Final v2); still open: header points chip, value-prop copy, kill "cheapest" — **NOW UNBLOCKED** by the publisher-compliance rule ([doc](publisher-compliance-copy-rule.md)). The kill list is concrete: `benefits_best_price` "Harga Terbaik" + its desc, `app.title` "Layanan Topup Termurah", and the `resmi`+`harga murah` half of `faq_answer_1`. Replacement value prop = "harga transparan + dapat poin tiap top up" (say what we give back, not what the price beats).
- [x] 2. "How it works" ✅ built as **Panduan Poin drawer** (`725:16668`) — mobile bottom-sheet, 5 concise sections (owner pivot 2026-07-22, see notes above). Tier table + full S&K deferred to T&C page #20. **✅ DESKTOP variant added 2026-07-29** — `Panduan Poin / Modal / D` (`964:19271`, 520×573) in overlay `964:19269`, cloned from the mobile sheet so the copy is shared. Use it for any desktop entry point (`Tentang poin` on the PRIME landing is the first; the wallet "Panduan" pill will need it too once #14 desktop lands).

### Earn flow
- [x] 3. Game / product topup page ✅ — strip (3 audience states, final copy implemented) + nominal card pill system + Figma component set (24 variants)
- [x] 4. Game detail PRIME upsell ✅ — superseded-by-design: dual-earn-per-card rejected; PRIME upsell = strip ("PRIME dapat 3x poin") + checkout drawer nudge
- [x] 5. Checkout / payment page ✅ — confirmation drawer (Figma set `Konfirmasi Pesanan / Poin`, 5 states) + redeem toggle in payment step (#6) + payment-extra footer strip
- [x] 6. Redeem toggle — **ON + OFF + coins-only + non-redeemable BUILT** ✅ (Figma: `Pilih pembayaran / Redeem Toggle ON` 601:12570, `OFF` 607:12570). Single-line card above method list: "Gunakan ◈ 1.250 poin" + brand-purple Switch (recolored from green, incl. check icon; purple = poin language, green reserved for events). ON state re-renders all method prices to remainder (Rp 12.104 → 10.854) — the key interaction. Subtitle dropped for compactness (owner; balance lives in profile).
  - **Coins-only BUILT, revised** ✅ (610:12699): amount = order total, toggle ON, **payment method section hidden entirely** + card subtitle "Pesanan ini dibayar sepenuhnya pakai poin" (supersedes dimmed-methods version — hidden beats 8 dead options). Interaction note for dev: toggling OFF re-expands the method section (smooth expand).
  - **Non-redeemable BUILT** ✅ (610:12570): toggle card hidden entirely, no reason shown (owner call) — labeled frame so devs know it's intentional.
  - **Payment-extra cashback FINAL (owner refine, 621:2204/2279) ✅:** footer strip under the method row, **dark-purple bg (poin surface family) + GREEN "1.5X" accent** — matches the boosted-pill grammar (purple container, green boost accent). Copy: "[1.5X] bonus poin dengan metode pembayaran ini". Solves dual-tag collision entirely: TERMURAH keeps its overhang slot, poin info gets a full-width line below. Supersedes all tag/chip/under-price attempts.
  - ⚠️ "1.5X" accuracy: only true at 1% base tier (1,25% tier → 1,4x; during 2x event → 1,25x). Label must be a CMS display field per campaign (never client-computed / hardcoded); safe alternative copy without number: "Bonus poin dengan metode pembayaran ini". Exact number still confirmed in the drawer sub-line ("ekstra dari [channel]").
  - Still open (spec only): (a) per-method disabled when remainder < method minimum (+ reason); (b) blocked by clawback debt; (c) 100%-discount voucher block. Data: `coins_to_use` + `balance_after_redeem` from API.
  - ❌ **RETRACTED 2026-08-20 — (d) DANA × poin-ON collision.** *(The premise is false: no method is barred from poin. Everything in this sub-block is historical; read the retraction note below it, not the bullets.)* ~~the one case that is NOT covered by #7's "absence explains it".~~ #7 is about a game that never offers the toggle, so nothing changes on screen and the user's inference is correct. **DANA is different: it's a payment *method*, and the toggle sits ABOVE the method list.** So the sequence is: game is redeemable → toggle appears → user switches it ON → **every method price re-renders down** (Rp 12.104 → 10.854, the key interaction) → user then picks DANA, which can't redeem → **the total jumps back up.** That is not absence, it's a reversal of something the user just did, with no explanation — they'd reasonably read it as a bug or a revoked discount.
    - 🚨 **RETRACTED 2026-08-20 — THE PREMISE WAS FALSE. Everything indented under this point describes a rule that does not exist.** The owner confirms no payment method is barred from poin; DANA was only ever an *example* of a non-eligible product, and eligibility is a per-product/game backend flag. The Figma work below has been undone: both `state=on` variants (mobile `631:12571`, desktop `1888:26058`) now show DANA as a normal `Rp 10.854` row, and the "DANA RULE (locked)" text is gone from the `632:12570` description. **What survives is the mechanism, re-instanced:** a method whose **minimum exceeds the remainder after poin** must be disabled with its reason visible — and production already ships exactly that (dim row + persistent `Minimal Rp 10.000` sub-line + **price retained**), so the "price is gone, not restated" ruling below is also wrong. Kept for the audit trail only:
    - ~~✅ **RESOLVED + BUILT 2026-08-03 (owner picked disable-with-reason).** DANA is **disabled whenever the poin toggle is ON**, with a reason in place of its price.~~ Built into `state=on` (`631:12571`) rather than as a 6th variant, because poin-ON *always* implies DANA-unavailable — it is what the ON state must look like, not a separate case. The `state=off` variant still shows DANA enabled at Rp 12.104, so the on/off pair tells the whole story.
      - Row `601:12635` → `Method / DANA (disabled — poin ON)`. Logo `601:12639` and label `601:12642` dimmed to **opacity 0.32**; the reason `601:12643` reads **"Poin tidak berlaku untuk pembayaran ini"** (owner wording, 2026-08-03).
      - **The reason occupies the `Minimal Rp 10.000` sub-line slot** (owner, 2026-08-03) — identical position and style: **x90 y23, Barlow Regular 12, `Surface 7`, left-aligned, full opacity**. Measures 206 wide, right edge 296 in a 319 row, so it holds one line with 23px spare. Longer strings will need a shorter phrasing, not a wrap.
      - This meant **flattening the row to the QRIS/OVO structure** — the AL wrapper `Frame 115505` and slot `Frame 2408` were deleted and logo / label / reason reparented as direct children of `Frame 115491`, matching every other method row. There is no longer a price node on this row.
      - The price is **gone, not restated** — showing Rp 12.104 next to four rows at Rp 10.854 would just raise "why is DANA dearer", and OVO already sets the precedent that a disabled method shows no price.
      - **The whole treatment now matches the existing OVO row exactly**, which was already built as a disabled method (logo + label at 0.32, sub-line at full `Surface 7`, price hidden). One disabled class, not two — and the sub-line being brighter than the label is the same hierarchy OVO already uses: the reason is the part the user must read.
      - Rule written into the set description on `632:12570` so it survives without this doc.
      - ❗ **Two pre-existing issues this surfaced, both still open.** (1) The 0.32 disabled treatment puts `Surface 8` at roughly **2:1** — below the contrast floor logged for #13b. It's the house pattern so DANA follows it, but the pattern itself should be re-cut (a `#5A5A5A`-class ink at full opacity clears 3:1 and reads the same). (2) **OVO is disabled with no reason at all** — the same "absence" failure this DANA fix exists to avoid. Whatever disables OVO should say so too.
    - Rejected: letting DANA stay selectable and turning poin OFF behind an explicit confirm — never silent, but it puts a dialog at the payment step and the total still jumps back up after confirming.
    - ~~Note `610:12570` is currently doing double duty for both the game-level and DANA cases.~~ **Moot 2026-08-20:** there is no DANA case, so `non-redeemable` carries only the game-level (product-eligibility) use — which is the settled one.
- [x] 7. Non-redeemable game state ✅ — toggle card hidden entirely, no reason shown (owner call; 610:12570).
- [~] 8/9. Order status page — **owner-implemented, TWO placements** ✅ (supersedes my standalone panels): (a) **inline "Poin Rewards" row inside PRODUK panel** (with Total — receipt reference, shown while in-progress); (b) **sticky bottom banner above "Beli Lagi"** — purple, "Kamu dapat +N poin dari pesanan ini" (always-visible hero, last thing before CTA). Two jobs: PRODUK = detail, sticky = visibility/celebration.
  - **State logic (owner-confirmed):** PRODUK inline row = in-progress only ("akan dapat", pending); sticky banner = **delivered only** ("Kamu dapat 265 poin dari pesanan ini" — tense correct, earned). Mutually exclusive, never both on screen. Delivered-to-earn expectation preserved.
  - **Sticky banner color = DARK + purple accent** (recommended over solid-purple): dark keeps the gold "Beli Lagi" CTA as the hero (solid purple competes), matches the poin-surface system (all other poin UI = dark container + purple accent), reads in both modes. Solid purple = celebration-burst language, for one-time moments not a persistent sticky. Keep purple border/glyph/bold number so it still reads as poin.
  - **Copy = TWO variants (owner-consolidated):** pending (Menunggu Pembayaran + Diproses, same copy) = "[N] poin didapatkan setelah pesanan selesai"; terkirim = "Kamu dapat [N] poin dari pesanan ini 🎉". 🎉 only on terkirim (never celebrate un-earned poin). Number purple. Mobile = dark+purple sticky above CTA; desktop = left-column card under the action card, width matched to QR/action card.
  - Note: numbers must derive from the same /preview value per txn (265 here vs 1.250 in earlier mock = placeholder difference only).
  - new-member celebration: fold into the delivered sticky banner (louder copy "Bonus member baru!"); no separate hero needed.
  - **Responsive rule (desktop, BUILT 657:15144):** no bottom sticky (mobile-only affordance) → poin = dark+purple card **in the LEFT column under the QR/status card** (owner call). Better than right-panel: fills the left column's whitespace (right panel is taller), and sits beside the pay action so the completion-nudge copy lands where the user acts. Cross-state rule: rides under whatever the left column's primary card is (QR / status / success). Mobile = sticky bottom, desktop = left-column card under the action card.

### Guest → member
- ~~10. Guest order-tracking claim CTA~~ — **DEFERRED** (tied to R3 guest escrow)
- ~~11. Login page~~ — **POSTPONED to V2** (owner). V1 uses existing login as-is; earn-nudge lives on strip/checkout/drawer.
- ~~12. Register form~~ — **POSTPONED to V2** (owner). Quick/easy redesign deferred; existing register stands for V1.

### Account / wallet
- [~] 13c. **DESKTOP PRIME subscription history + voucher page BUILT (owner, 2026-07-29):** `972:12084` **Riwayat Langganan** · `972:13189` **Voucher PRIME** (both 1440×1803, PRIME left column, same shell).
  - ✅ **Subscription history converts the mobile card stack into a real TABLE** — Tanggal Transaksi / Status / Periode Aktif / Metode Pembayaran / Total, right-aligned money, GoPay logo + masked phone vs card glyph + masked PAN. Correct desktop move. **✅ "Gagal" status now present (row 3) — closes the row-status-variant item I raised on `967:19481`.** Pending still absent.
  - ✅ **Voucher page** = 2-col grid, active cards vs dimmed spent/expired cards, "Voucher PRIME" panel. Copy "Diskon 50% … untuk Mobile Legends" **passes the compliance rule** (diskon scoped to a named voucher). **Partially delivers #24 My Coupons for the PRIME-voucher case.**
  - ❗ **Three bugs I flagged on the mobile frame carried straight over to the table:** **"31/2/2025"** (31 February, row 1) · **"Rp 20,000"** — still comma-separated where the site uses periods, and still 20,000 where PRIME is **Rp 25.000** · **numeric D/M/YYYY dates** ("1/3/2025", "19/12/2024") in a frame whose own header and left column both say "8 Jan 2026".
  - ❗ **The history table isn't in date order.** Rows run 1/3/2025 · 21/1/2025 · 20/1/2025 · 19/12/2024 · then alternate 20/1/2025 / 19/12/2024 repeatedly. It's a *history* table — reverse-chronological is the whole contract, and a dev will mirror whatever order the mock shows. Add sort affordance + `aria-sort` while you're there, and tabular figures on Total.
  - ❗ **Voucher: dimmed cards carry no state label.** Dimming alone is doing the work of distinguishing **used** from **expired** — two states the user cares about differently — and their contrast is **1.96:1 (title) / 1.7:1 (date)**, i.e. unreadable, on content the user needs in order to know *which* voucher lapsed. Add an explicit badge ("Terpakai" / "Kedaluwarsa") and lift the text to ≥4.5:1.
  - ❗ **Voucher count contradicts the grid** — sidebar cell says **Voucher 8**, grid shows **10** (4 active + 6 dimmed). Same class of defect as the poin card's 128.000 vs Rp 12.000: a stat cell disagreeing with the list it links to.
  - ⚠️ **"--- Semua data sudah ditampilkan ---"** is a debug string, not product copy. Elsewhere the pattern is a "Tampilkan lebih banyak" pill that simply disappears when exhausted.
  - ⚠️ **Voucher titles truncate mid-word** ("…Mobile Legends : Ba…") with room for a third line. Rule is wrap before truncating; if truncating, expose the full text.
  - ⚠️ **"Diskon 50% up to 20RB"** — English "up to" inside Indonesian copy ("hingga" / "maks."), and "20RB" against the site's "Rp 20.000" convention.
  - ⚠️ **"Lihat" vs production's "Pakai"** on the voucher button — different verb, different promise. The live PRIME page uses **Pakai**. Confirm which this page does.
  - ⚠️ All four active vouchers are identical duplicates — reads as a rendering bug in a 2-col grid. Vary the placeholders.
  - Voids: history 472px (content ends 848), voucher ~556px. Same as #13b.
- ✅ **DEFECT SWEEP APPLIED (2026-07-29) — all five desktop frames, verified on re-render.** Fixed:
  - **`972:9896` Poin Section** — "kadaluwarsa" → **"kedaluwarsa"** in **both** places (`972:10655` ledger row *and* `972:10780` balance card — note: both were wrong; my earlier claim that the ledger spelled it correctly was mistaken) · `972:10566` "lebih murah" → **"lebih hemat"** · all ledger dates abbreviated to "3 Jul 2026" / "28 Jun 2026" style via **range-safe edits** so the gold "Pending" and "Pesanan Dibatalkan" runs survived · **PRIME-bonus tile `972:10621` → gold** (tile `Rewards Surface 6`, glyph `#E09A22`/`#FBD37A`/`#B87A18`) restoring gold=PRIME · **expiry tile `972:10652` → most muted** (`#262630`, glyph grays) so the icon now agrees with its `−120`.
  - **`970:7473` + `970:9121` Edit Profile** — EMAIL field `Surface 6`/`Surface 5` (**2.06:1**) → `Surface 2` fill + `#3A3A3A` border + `Surface 7` ink (**~6:1**), still clearly read-only · **Simpan → gold `Primary`** with `Surface 5` Barlow Black label. ⚠️ **Trap for next time:** the pill's fill lives on a child `Rectangle 4346`, not the `Frame 115535` wrapper — setting the frame fill looks applied in the API and changes nothing on screen.
  - **`972:12084` Subscribe History** — all 10 rows re-dated to a valid **descending** monthly series (1 Mar 2025 → 1 Jun 2024), killing **"31/2/2025"** and the out-of-order rows · **Rp 20,000 → Rp 25.000** ×10 · date cells switched to auto-width (fixed 75px was wrapping "1 Nov 2024") · "Berhenti berlangganan" `Surface 4` on `Surface 1` (**1.05:1**) → `#2B2B2B` + `#5A5A5A` border.
  - **`972:13189` Vouchers** — spent-card opacity **0.32 → 0.6** (text was 1.7–1.96:1); confirmed the separate 0.32 `972:13192` group is **not** an ancestor so nothing compounds · **new "Kedaluwarsa" state badges** on all 6 spent cards (`984:21512`–`984:21522`), placed in the un-dimmed parent so they stay legible and sitting exactly where "Lihat" sits on active cards — state is no longer conveyed by dimming alone · `--- Semua data sudah ditampilkan ---` → **"Semua voucher sudah ditampilkan"** · titles → **"Diskon 50% hingga 20RB untuk Mobile Legends"** (fixes the English "up to" *and* the mid-word clipping, which turned out to be parent clipping rather than truncated copy).
  - **Owner had already fixed the balance** — `972:10742` reads **12.000** and matches both the equivalence line and the sidebar cell.
- ❗ **STILL OPEN on these frames (deliberately left — judgement calls, not defects):** **"Lihat" vs production's "Pakai"** on voucher buttons · **"Voucher: 8" vs 10 cards** — the fix depends on whether that cell means *available* or *total*, which is a semantics decision · the **472–576px voids** (needs a call on whether the footer rides up or the pages get filler) · **Pending** status still absent from the history table · propagating **"Khusus PRIME"** to mobile `660:4117`.
- 🔻 **Contrast floor to hold going forward:** interactive surfaces **≥3:1** against their background, any text the user must read **≥4.5:1**. In-system answers: gold `Primary` for primary, `#5A5A5A`-or-lighter border for secondary. The four failures above all came from de-emphasis applied without a floor.
- [~] 13b. **DESKTOP account shell + Ubah Profil BUILT (owner, 2026-07-29):** `970:7473` **Member Edit Profile** · `970:9121` **PRIME Edit Profile** (both 1440×1803) · `970:7093` **RRQ Topup Prime Status Active** (1440×1580).
  - ✅ **Shell matches production exactly** — header 64 w/ centred icon+label tabs · left column **375** (x122–497) · gap **35** · right panel **786** (x532–1318) · container **1196**, gutters 122 · page `Surface 1`, panel `#1D1D1D` · gold marquee y1320 + `Footer / D`. **This is now the canonical desktop account shell — build the wallet desktop (#14) into it.**
  - ✅ Member vs PRIME differ **only in the left column** (silver profile card + "Upgrade ke PRIME, dan dapatkan 3X poin lebih banyak daripada member biasa" upsell row · vs gold-gradient card + "3X" chip on the Poin cell + "Aktif / Perpanjang otomatis: 8 Jan 2026" row → `967:19481`). Right panel identical. Economical and correct.
  - ✅ **Upsell copy is on-message and compliance-safe** — multiplier framing, no discount, and "lebih banyak daripada **member biasa**" is a comparative with a **named internal** comparison set, so it passes the publisher rule. PRIME "3X" gold chip matches #13.
  - ❗ **EMAIL field fails contrast badly — 2.06:1.** Fill `Surface 6` with `Surface 5` ink, while the other three fields are `Surface 8` fill / `Surface 5` ink (14.31:1). The intent (non-editable) is right, the execution makes the address unreadable — and this is information the user needs to confirm which account they're on. Also ambiguous whether it's **disabled or read-only**, with no reason given. Fix: keep it non-editable but legible — e.g. `Surface 2` fill with `Surface 7` text (~6:1), plus a lock icon and a one-line hint ("Email nggak bisa diubah — hubungi CS kalau perlu ganti").
  - ❗ **"Simpan" is the page's only action and its button is invisible.** Pill fill `Surface 2` on a `#1D1D1D` panel = **1.02:1 boundary** — the label reads (12.14:1) but the affordance doesn't. Site convention for a primary CTA is gold `Primary` (Lanjut Berlangganan / Login Sekarang / Berlangganan Sekarang / Tutup). Make it gold.
  - ❗ **576px empty void.** Content ends y744 (left column; right panel ends y468), marquee starts y1320. Frame is 1803 tall where ~1225 is needed. Bring the footer up rather than inventing filler — a 4-field form is legitimately short.
  - ⚠️ **The main column is emptier than the nav column** (right panel 404 tall vs left 620). Reads unfinished. Either accept it as a short section page, or move something profile-adjacent into the right column (password / Keamanan akun is the natural sibling of profile editing).
  - ⚠️ **"+62" in NO. HANDPHONE** — unclear whether that's the value or a prefix placeholder. Needs a separated country-code affordance or a real value.
  - ⚠️ **No form states drawn:** validation/error, required-field markers, Simpan disabled + loading, success feedback. See the rules already logged for #19 error states.
- [x] 13. Account dashboard ✅ (owner-built final, both states) — **unified layout**, Poin + Voucher two side-by-side stat cells in the profile card (replaces dead "Total hemat"); name beside avatar (compact). Regular: silver profile card + plain cells + PRIME upsell row "Upgrade ke PRIME, dapatkan 3X poin". PRIME: **entire profile card = gold gradient** (gold-membership-card flex — acceptable as a single hero element, unlike a repeating grid) + **"3X" gold chip on the Poin cell** (explicit multiplier tie) + "Aktif · perpanjang otomatis" row. Poin glyph purple in both, sits in a dark cell (never directly on gold → purple=poin law holds). Poin cell = tap → wallet. Expiry hint deferred to wallet page (must appear there — retention engine).
- [~] 14. RRQSHOP Rewards wallet page — **BUILT** ✅ (Figma 676:16195, RRQSHOP Rewards page, mobile). Site-accurate: panel chrome cloned from real panels (`Surface 4` bg, corner rivets, hazard-stripe Barlow-Bold titles). SALDO card = **GoPay-Coins-style** (owner ref): rounded card, purple dot-grid ornament top-right, glyph + "RRQ Shop Rewards" label, big "12.000" (Barlow Black), "⇄ Setara dengan Rp 12.000" (reinforces 1:1 peg), dashed divider, gold expiry "350 poin hangus dalam 12 hari" (retention hook), pill "Cara kerja RRQ Shop Rewards →" (→ how-it-works #18/#20). Minor: page-header title duplicates card label — consider header = back+help only (GoPay pattern). RIWAYAT panel (owner frame 681:16812) = **flat rows w/ game thumbnail** (40px, left) + title + date + amount right (colored: green earn, red spend, gray pending+expired), full-width hairline dividers, no nested card. Rule: earn/spend rows = game thumbnail (txn tied to a game); pure-poin rows (expiry/bonus, no game) = muted glyph tile. From /history + /balance API.
  - **Decisions (owner):** ~~no "+" on earn~~ · ~~Pending NOT shown~~ → **REVERSED 2026-07-22** (see "Wallet page restructured" note below: earn now "+" purple, pending now shown). Kept: expired more muted than spent. **Expiry copy conditional: >2 weeks → fixed date "30 Des 2026" (calm); ≤2 weeks → "hangus dalam 14 hari" (amber, urgency)** — restores urgency exactly when near; makes expiring-soon notif (E2) the far-out retention lever.
  - **States BUILT ✅ (all consistent, owner-refined 2026-07-22 — auto-layout wrapper 351@x12 gap12, collapsed Informasi Poin panel, new +purple sign convention):**
    - **empty** (706:12570) — SALDO balance 0, no expiry, "Mulai top up game favoritmu untuk mulai kumpulin poin." · Riwayat empty "Belum ada riwayat poin nih" · Informasi Poin collapsed (729:18311) · wrapper 740:18368.
    - **debt** (708:12570, owner-finalized) — SALDO **balance "−500" purple `Rewards 4`** (Barlow Black 28, NOT red — alarm carried by banner not the number) · "Setara dengan Rp 0" · debt-aware subtitle "Poin kamu minus nih, top up berikutnya akan dipotong." · Riwayat ⚠ banner "Poin dikurangi −500 karena pesanan direfund. Poin dari transaksi berikutnya akan dipotong untuk melunasi kekurangan ini." · Informasi Poin collapsed (740:18479) · wrapper 740:18464.
      - **Owner decision (2026-07-22): debt = rare edge case → NO special design beyond this. Do not elaborate further; color-law stays as-is (no red-negative variant needed).**
    - PRIME bonus-lot row (709:12578 in main ledger — gold PRIME glyph tile + "Bonus langganan PRIME" + purple amount).
  - ✅✅ **DESKTOP BUILT (owner, 2026-07-29): `972:9896`** — "poin section in profile page", 1440×1803, in the canonical account shell (#13b): left col 375 member variant, right col 786. **Structure is right and reuses the patterns as intended:** balance card as a **two-cell row** (glyph + "RRQSHOP Rewards" + big number + "Setara dengan Rp…" left · expiry + "Panduan Poin" pill right, purple dot ornament between) with a **purple glow border** that finally gives the balance proper hierarchy and stays on-law · **Informasi Poin as a 2×3 grid**, row-major, preserving mobile order 1–6 · **Riwayat Poin** full-width list with game thumbnails. Void is only 176px (content ends 1144, marquee 1320) and the right column is now longer than the left (1144 vs 744) — both better than the edit-profile frames. "Panduan Poin" pill → wire to the desktop modal `964:19271`.
    - ❗ **THE BALANCE NUMBERS CONTRADICT EACH OTHER — three ways.** Card reads **128.000**, its own equivalence line reads **"Setara dengan Rp 12.000"**, and the left sidebar Poin cell reads **12.000**. The 1:1 peg is a locked rule and reinforcing it is this card's entire job, so this is the one thing that must not ship. Pick one number and make all three agree (128.000 → "Rp 128.000", or balance → 12.000).
    - ❗ **"kadaluwarsa" is misspelled — and inconsistently within the same frame.** The balance card says "Poin akan **kadaluwarsa** pada 30 Des 2026"; the ledger row 6 rows below says "Poin **kedaluwarsa**". KBBI-correct is **kedaluwarsa**. Fix the card.
    - ❗ **PRIME bonus row uses a purple tile, breaking the gold=PRIME law.** `#683D8F` purple glyph tile on "Bonus langganan PRIME" — the locked treatment (mobile `709:12578`) is a **gold** PRIME glyph tile. As drawn it's indistinguishable from the "Poin kedaluwarsa" tile.
    - ❗ **The expiry tile is the brightest thing in the list.** "Poin kedaluwarsa" tile is `Rewards 3` — brighter than the PRIME bonus tile — while its amount (−120) is correctly the most muted. Icon and amount pull in opposite directions. Rule is: pure-poin non-game rows get a **muted** glyph tile, and expired is the most muted row overall.
    - ⚠️ **"Pending" label is gold** — gold is reserved for PRIME by the colour law, and here it sits inches from real PRIME gold in the same panel. Amber is already spec'd for near-expiry, so amber is effectively becoming "time-sensitive" rather than PRIME-only. Either document that explicitly and make the two golds visually distinct, or render Pending in the muted gray.
    - ⚠️ **Date format split:** ledger rows use full months ("3 Juli 2026") while the balance card uses abbreviated ("30 Des 2026") and the PRIME pages use "8 Jan 2026". Standardise on abbreviated — it's what the rest of the product uses and it keeps rows compact.
    - ⚠️ **`Informasi Poin` item 1 is the closest copy on the page to the compliance line:** "…supaya transaksi jadi **lebih murah**". It passes (the mechanism — paying with poin — is named in the same sentence, so the comparison set is bounded), but `murah` is the flagged family. Swap to **"lebih hemat"** — zero cost, moves it clearly inside the line.
    - ~~⚠️ Program name drift: this card says "RRQSHOP Rewards"; this doc's header still says "RRQ Shop Rewards". Pick one.~~ **RESOLVED 2026-08-20: RRQSHOP Rewards is correct** (copy law, HANDOFF §2); this doc's header and item titles were corrected.
    - ✅ **Voucher cell for NON-PRIME = "Khusus PRIME"** (owner, 2026-07-29, gold on PRIME). Good call: a member has no vouchers, so a count of "8" there was both wrong and confusing. Keeps the two-cell pair symmetric with the PRIME variant instead of dropping the cell, and the gold is on-law. **Follow-ups:** (a) **propagate to mobile** `660:4117` (Akun - Regular) and any other member-state surface carrying the Poin/Voucher pair — otherwise the breakpoints now disagree; (b) **declare the tap target** — the Poin cell taps through to the wallet, so this cell should now route to the PRIME page since it's become an upsell surface; (c) it's the **second PRIME pitch within ~40px** (the "Upgrade ke PRIME…" row sits directly beneath) — acceptable, but consider dropping the type a step since a status string at number-scale makes the two cells look like comparable data.
  - Still open: **near-expiry ≤2wk amber variant on desktop too** (`972:9896` shows the calm far-date variant) · **PRIME variant of the desktop poin page** (left column gold card + right column purple-glow card = two glowing cards competing; check when built).
  - 🎯 **DESKTOP DIRECTION SETTLED (2026-07-29) — slot into the account shell, don't invent one.** Confirmed twice: production `/prime` (owner screenshot) **and** the owner's own Figma frames `970:7473` / `970:9121` / `970:7093`, which measure **container 1196 @ 122 gutters · left column 375 · gap 35 · right panel 786 · header 64 with centred icon+label tabs · page `Surface 1`, panel `#1D1D1D` · flat dark bg + watermark, NO gradient hero band.** Build #14 desktop into that shell — see **#13b** for the reference frames.
    - **Wallet becomes a 5th sidebar item ("Poin Saya").** Not a new page shell.
    - ⚠️ **Correction to my first pass:** I derived the desktop grammar from `767:4297` (txn history) and got a 1440×465 gradient hero band + centred title + 1024 container. **That's the landing-style pattern, wrong for the account cluster.** Account pages use the sidebar shell above. The 1024 container idea is dropped.
    - **Content column reuses three patterns that already exist on the live PRIME page** — a two-cell stat row (there: "8/12 Voucher" | "Rp 12.000"), a **3×2 icon grid** (there: the Benefit panel), and a full-width list. So: SALDO = two-cell row (balance + "setara" left | gold expiry + "Cara kerja" pill right) · Informasi Poin = its 6 rows as the 3×2 grid · Riwayat = full-width list + "Tampilkan semua". Nothing new invented.
    - **Why modules must not be stretched to 784+ blindly:** corner rivets are 8px at fixed 8px insets, so the rivet-to-width ratio collapses past ~2× mobile width; hazard-stripe headers turn from accent into banner (hit this for real building the Panduan Poin modal at only 520); and body copy at 784+ blows past 65–75ch. 784 ≈ 2.2× the 351 module is the practical ceiling — hence the two-cell / grid / list patterns rather than one wide slab.
    - ⚠️ **Mobile nav pressure:** mobile replaces the sidebar with a **segmented control** ("Akun Saya | Langganan PRIME | Akun Game"). Adding a 4th segment at 375 will be tight — decide whether Poin becomes a 4th segment, or stays reached from the account card's Poin cell (per #13) with no segment.
    - Breakpoints: 1 column below 1024, sidebar at ≥1024.
- [x] 15. Transaction history (Riwayat Pesanan) — **DONE: auto-layout component set (mobile + desktop) + swapped into both layouts** ✅ MOD + owner-refined card template (orig frame 767:17575; **new card template `771:18087`, owner 2026-07-22**). Poin-row concept retained, card restructured by owner: order ID moved to TOP (under status pill) · "Total belanja" label + price (bottom-left) · gold "Beli Lagi" CTA (bottom-right) · **poin row bottom-left** = "◈ +N poin setelah pesanan selesai" (muted glyph+text = pending). Per-status poin logic (from MOD): Selesai+eligible = purple "+N poin" (earned) · Diproses = muted "+N poin setelah pesanan selesai" (pending) · voucher/non-game = none (eligibility) · Belum Dibayar/Dibatalkan = skip. Numbers placeholder; production from /preview.
  - **COMPONENT SET v1 (absolute layout, superseded):** `776:17369` — kept as reference; replaced by AL set below. (Safe to delete.)
  - **COMPONENT SET v2 — AUTO-LAYOUT ✅ (2026-07-22):** `Pesanan Card / Mobile (AL)` (`825:17404`). Proper AL (Header transparent w/ pill protruding + Body Panel `Surface 4` absolute under topbar + Card content AL: ID / ProductRow / Divider / Footer[ValueCol + button] + rivets absolute pinned corners). Props: **`Status`** (5: Belum Dibayar/Diproses/Terkendala/Selesai/Dibatalkan) · **`Pakai Poin`** boolean → toggles redeem line "– Pakai N poin" (all except Dibatalkan) · **`Beli Lagi`** boolean → toggles gold compact button (Selesai/Dibatalkan). Poin: pending "+N poin setelah pesanan selesai" (Belum Dibayar/Diproses/Terkendala) · earned "+N poin" purple (Selesai) · none (Dibatalkan). Resilient: long names wrap, redeem line reflows, numbers swap safe. Auto-layout structure was iterated w/ owner + a Figma Agent; poin-text width bug (FIXED 211 → HUG) fixed. Numbers placeholder; production /preview. Thumbnail art = placeholder per game.
  - **DESKTOP set ✅:** `Pesanan Card / Desktop (AL)` (`829:17404`) — cloned from mobile AL set, widened to 375; AL auto-reflowed (FILL children + corner-pinned rivets). Same props (Status / Pakai Poin / Beli Lagi). Beli Lagi = compact gold right (same as mobile, owner-corrected from earlier full-width idea).
  - **SWAPPED into layouts ✅:** mobile txn history (list 767:17597, 5 instances) + desktop layout (767:4297, grid 3+2, 5 instances). Old absolute set 776:17369 deleted.
  - Redeem "pakai N poin" = handled by `Pakai Poin` boolean prop (not a separate variant).

### PRIME reposition
- [~] 16. PRIME sales / subscription page — **OWNER REWROTE THE CONTENT IN FIGMA (2026-07-29), node `858:17959`** ("Landing Page", 1440×2347, page "RRQSHOP Rewards" @ x14972 y6100). Full copy diff vs live `/id-id/prime` → **[prime-landing-figma-vs-prod.md](prime-landing-figma-vs-prod.md)**.
  - **This is the reposition landing as copy.** Structure untouched (same sections/grid/CTA); headline + subcopy rewritten, **3 of 6 card titles and 6 of 6 card descriptions** rewritten, one FAQ item deleted.
  - **Headline:** `Top-up hemat maksimal, pengalaman jadi spesial` → **`Top up seperti biasa, poin 3x lipat.`** (2 lines, Barlow Black). Strongest edit on the page — makes **no price claim at all**; "seperti biasa" parks price as unchanged and puts the whole benefit on the return.
  - **Card 1 flipped from discount to poin:** `Selalu ada diskon` / "Diskon untuk semua item di website RRQ Topup" → **`Poin 3x lipat`** / "Poin kamu dihitung 3x tiap top up di game yang dapat poin, tanpa nunggu event." **+ new purple `Tentang poin ⓘ` pill** (`961:19344`). Other retitles: `Dapatkan voucher promo`→`Kupon eksklusif PRIME`, `Laporan hemat bulanan`→`Laporan poin bulanan`. Cards 4/5 rename RRQ Topup→**RRQSHOP** (confirm the rename is intended + global, not partial).
  - **Colour law OK** — card 1's gold 3X badge + purple "Tentang poin" pill matches the #13 account-dashboard precedent (gold = the PRIME multiplier, purple = poin). Looks like a violation at a glance; isn't.
  - ✅ **Compliance:** this update fixes **F9, F10, F11** and the borderline `hemat maksimal` headline, and **introduces no new failures** (`diskon` now always scoped to "member PRIME"). **Still open, not on this page:** F7 `suggest_title` "Dapatkan Harga Lebih Murah", F8 `suggests_desc`, and **`saved_price_product` = "Diskon PRIME"** (per-product label — still the old model, and it collides with the built nominal-card PRIME treatment in `529:895`, which is a gold 3X chip + strikethrough poin pill, not a price discount).
  - ✅ **RESOLVED (owner, 2026-07-29): PRIME's blanket discount is RETIRED — replaced by the poin multiplier.** Owner: *"there's no more extra discount for PRIME, because we change it into poin multiplier."* **The landing page is correct as built** — card 1 is right, no 7th card needed. **Scoped discounts survive:** cards 2 + 3 (Flash Sale PRIME, Kupon eksklusif PRIME) are still true and must NOT be stripped too.
    - ⚠️ **This is a material change to a live auto-renewing paid subscription** — existing subscribers bought "selalu ada diskon" at Rp 25.000/bulan. Needs **subscriber notice before next renewal + PRIME T&C update + a grandfathering decision**. Design can't answer those; copy shouldn't ship before they're answered.
    - ⚠️ **Check 3x poin is worth at least the old discount — and note it's less liquid either way.** Old blanket rate unknown. Even at nominal parity (old 3% discount vs 3% poin rate), poin are **strictly worse in practice**: a discount is immediate and unconditional, poin **expire in 60 days**, are spendable only on RRQSHOP, and are lost if the user stops topping up. If the old rate exceeded the new `prime_rate`, some subscribers are plainly worse off. That comparison is the first thing a subscriber will run — do it before the notice goes out.
    - **`saved_price_product` = "Diskon PRIME" must die** (per-product label, ships the retired model). Replacement already built: nominal-card set `529:895` shows PRIME as **gold 3X chip + strikethrough poin pill** (`◈ +105 ~~+35~~`). This is what makes product cards agree with the landing page.
    - **F7/F8 are now false, not just non-compliant** — `suggest_title` "Dapatkan Harga Lebih Murah" + `suggests_desc` "diskon terus-terusan" promise a discount that no longer exists.
    - **Check the checkout "Hemat" line** (`saved_price_order_detail`) no longer computes/displays a PRIME discount component for PRIME members.
    - ⚠️ **TWO MORE LIVE CASUALTIES, found on the production `/prime` account page 2026-07-29 (the landing-page diff did NOT cover this surface):** (1) a **"Rp 12.000 / Total penghematan"** stat cell — a savings-from-discount metric that becomes meaningless once the discount is gone; note the to-do claimed #13 "replaces dead 'Total hemat'", but this surface still ships it. (2) the **"Benefit berlangganan RRQ Topup PRIME:" panel still lists the old 6 benefits including "Selalu ada diskon"** (= the F11 string) in a 3×2 grid. Both need the new poin-multiplier copy from `858:17959`. **Add this page to the reposition scope.**
  - ✅ **RESOLVED (owner): `Tentang poin ⓘ` → triggers the Panduan Poin drawer `725:16668`.** Mobile done (pill `962:19345` → existing bottom-sheet, nothing built).
  - ✅✅ **DESKTOP MODAL BUILT (2026-07-29): `Panduan Poin - D`.** Overlay frame **`964:19269`** (1440×900, page "RRQSHOP Rewards" @ x16500 y6100) + scrim `964:19270` (black 60%) + modal **`964:19271`** `Panduan Poin / Modal / D` **520×573**, centered 460,164.
    - **Cloned from the mobile sheet `725:16998`, not rebuilt** — chrome, paints, hazard-stripe headers, gold Tutup pill and all 5 sections of copy carry over byte-identical. **Zero new copy, so mobile and desktop can't drift.**
    - Reflow spec: width **520** · gutter **24** (content 472) · title bar 52 · content top 68 · section gap 20 · gap-to-button 32 · bottom pad 24 · sheet radius **16 all four corners** (bottom-sheet was top-only) · Tutup 166×44 centered. Body text was already `textAutoResize: HEIGHT` so sections re-wrapped themselves → 105/65/45/65/45.
    - ⚠️ **Trap if anyone redoes this:** each header's hazard-stripe frame was **375/351 wide inside a 343 strip** — already overflowing on mobile. Widening the strip alone leaves the bar ending short of the text; the inner frames must be resized *and* set `clipsContent` so the pattern crops. (Also: `725:16668` has a title bar + X close, **no grabber** — nothing to remove.)
    - **Layers named from their own copy** while in there (the recurring handoff cost on this page): section frames are now `CARA Dapat poin`/`Hitungan poin`/`MEMBER PRIME`/`MASA BERLAKU POIN`/`PAKAI POIN` with `Header`/`Body`/`Hazard Stripe` inside, plus `Title Bar`/`Close`/`Sections`/`Tutup Button`. Hidden `Lanjut Bayar` deleted rather than cloned forward.
    - **No copy change needed from the retired-discount decision** — the drawer's MEMBER PRIME section already reads "Member PRIME dapat 3x poin tiap transaksi — tanpa perlu nunggu event" (multiplier-only).
    - **Dev wiring:** `Tentang poin ⓘ` → this modal at ≥768, → bottom-sheet `725:16668` below. Pills: desktop `961:19344`, mobile `962:19345`.
  - ⚠️ **Two live FAQ answers the reposition makes wrong:** (1) **FAQ 3** "Apakah ada batasan penggunaan untuk benefit PRIME?" → prod answers **"Tidak ada"**, now false — there's a global max-rate ceiling (~4%), per-user event caps, and **PRIME + coupon auto-downgrades to member rules**; must also agree with #23's confirmation popup. (2) **FAQ 9** cancellation → doesn't say **what happens to poin already earned at 3x** (they're kept: earning is per-txn, expiry is wallet-level) — an unanswered "do I lose my poin if I cancel" on the cancellation FAQ is exactly what suppresses subscription.
  - ⚠️ **FAQ 10 deleted** ("Kemana saya bisa melaporkan jika ada masalah?" + CS email/WA/registered address). Flagged as probably-not-intended: it's the complaint path on the one page where money recurs. Content *is* stale though — says `PT Qeon Interactive`, footer now says `PT MID DIGITAL INTERACTIVE (formerly PT QEON INTERACTIVE)`. Updating beats deleting.
  - **`Tentang poin ⓘ` needs a destination** — Panduan Poin is a *mobile bottom-sheet* drawer (`725:16668`); this is a 1440 desktop page with no desktop equivalent. Build one, or point at #20.
  - **Figma hygiene:** headline (`858:18178`), both section titles (`858:18015`/`858:18180`) and every FAQ text node are still named **`Game Name`** — had to render the frame and read pixels to recover the headline. Also `858:18147` hidden "Lanjut Bayar" inside the CTA named `Pilih pembayaran` (same artifact as the Rewards hero CTA), plus ~13 dead hidden layers. Rename before handoff.
  - **Locale-key note:** rename `benefit_discount_*` rather than re-valuing it — a key called `discount` holding poin copy is how F11 gets reintroduced.
  - ✅ **MOBILE ALSO UPDATED (`858:18217` "Landing Page - M", 375×3226).** Copy parity with new desktop is **exact** — headline, subcopy, CTA + footnote, both section titles, 5/6 card titles, 5/6 bodies, the `Tentang poin ⓘ` pill, all 9 FAQ questions verbatim, FAQ 10 dropped on both. Layout already matches prod: 2-col grid **164px @ x18/x194** vs prod **167px @ x16/x193** (within 3px), so no layout work needed. **3 bugs + 1 naming issue:**
    - ✅ **M1 FIXED** (owner, verified on the render) — mobile card 1 now shows the gold **`3X`** rosette (new node `962:19351`). Was the old `%` discount rosette sitting directly above the words "Poin 3x lipat".
    - ✅ **M2 FIXED** — mobile card 5 now `…tim CS RRQSHOP.` (`858:18411`). ✅ **Trailing periods now consistent** — all 6 card bodies end with `.` on both breakpoints (card 4 gained one on both, card 5 on desktop).
    - ❗ **M3 — STILL OPEN, not touched by the copy pass. FAQ chevron y is `h/2 + 10` where centring wants `(h − 20)/2`** → consistent **+20px** drift on mobile (+17 desktop), and on the single-line row (`858:18264`, h38) the chevron spans to y49 and **overflows the row bottom by 11px**. Taller rows drift further, so the list reads progressively misaligned. Targets: h38→y9 · h64→y22 · h84→y32 · h104→y42 · desktop h58→y19. Almost certainly fallout from the 07-27 FAQ responsive pass (`FAQ / D / Closed` `858:17954` is read-only, rows were **detached** to become resizable, and a detached chevron's y doesn't follow a height change) — **check the desktop FAQ panel `883:6642` for the same drift.**
    - ❗ **M4 — mostly fixed, 2 stragglers.** Mobile renamed 5 of 6; **mobile card 1 `858:18317` is STILL named `Selalu ada diskon`** — the one that matters most, because that string **is F11**, so a dev grepping to confirm its removal still gets a hit. Desktop card 3 `858:18056` still `Dapatkan voucher promo` vs its text `Kupon eksklusif PRIME`. Minor: mobile cards 3/4 frames use lowercase `prime` (`Kupon eksklusif prime`, `Giveaway khusus prime`) while text + all other frames use `PRIME`.
    - ❗ **`Game Name` placeholders unchanged everywhere** — headline both breakpoints (`858:18178`, `858:18248`), all 4 section titles, every FAQ text node, mobile bottom-bar label `858:18535`. Icon layers are still export hashes both sides (`…a1 1` desktop / `…a1 2` mobile) — differing suffix now signals two assets, but neither name says `3X`.
    - Minor: row heights 251/237/228 hand-tuned not auto-layout (editing body copy = manual re-tune) · `858:18256` panel bg h702 inside `858:18255` h518, 184px overflow (desktop same: h482 in h284 — harmless, but frame height isn't a usable spec) · same `Game Name` placeholders + same hidden `Lanjut Bayar` (`858:18447`) as desktop.
- [~] 17. PRIME status in account — **original brief ("show multiplier") is DONE, absorbed by #13.** Owner's PRIME account state (`660:4746`) already carries the gold gradient profile card + **"3X" gold chip on the Poin cell** (explicit multiplier tie) + "Aktif · perpanjang otomatis" row, and it replaced the dead "Total hemat" cell. #17 was written 2026-07-02, before #13 was built; nobody closed it. **Do not re-design the multiplier display.**
  - **REPURPOSED — two real gaps this surface still owns:**
  - ✅ **(a) SUBSCRIPTION MANAGEMENT — BUILT (owner, 2026-07-29): `967:19481` "PRIME History - M"** (375×2605). Entry point = the **"Aktif" row on the PRIME account page** (`660:4746`) → this page. Correction to my earlier note: the renewal date **was** already there ("Perpanjang otomatis: 8 Jan 2026" on the account row) — I was wrong that it wasn't.
    - Contents: header "Perpanjang otomatis: 8 Jan 2026" + ghost pill **"Berhenti berlangganan"** (`967:19972`) top-right · panel **"Riwayat Langganan"** = 10 rows (date · amount · "Langganan untuk [start] - [end]" · GoPay logo + masked `08********1776` · status "Sukses") · "Tampilkan lebih banyak" pill · `Footer / M`.
    - ✅ **CANCEL CONFIRMATION BUILT + REVISED (owner, 2026-07-29): `967:19975` "Unsubscribe Prime"** (375×**224**). Title "Berhenti Berlangganan?" · body **"Benefit PRIME tetap aktif hingga 8 Jan 2026, lalu akunmu jadi member biasa. Poin yang sudah kamu dapat tetap aman. Periode yang sedang berjalan nggak bisa direfund."** (4 lines, h80) · **"Ya, Batalkan"** quiet `Surface 2` (left) / **"Nanti Lagi"** gold `Primary` (right).
      - **Revision history for context:** v1 had a one-fact body and a **red gradient `#7F0C1C→#F94549` "Konfirmasi" on the right** — the loudest element in the dialog, which made cancelling read as the recommended action. v2 inverted the hierarchy (gold safe-action on the right, matching the `629:15489` precedent) and added all four facts. **Both fixed.** Note for the record: my v1 objection also claimed red broke the colour law — **that was wrong.** Line 89's "red+(−) = spend/expired" clause was superseded by the 07-22 wallet restructure (spend `Surface 7`, expired/canceled `Surface 6`, debt balance deliberately purple "NOT red"). Red is unused in the poin system and is free for destructive.
      - ✅ **v3 (final): both nits fixed.** Label → **"Ya, Berhenti"** (v2's "Ya, Batalkan" was ambiguous — *Batalkan* is the **dismiss** verb in Indonesian UI, so it read as "cancel this dialog" as easily as "cancel my subscription", right next to "Nanti Lagi" which genuinely IS the back-out; "Ya, Berhenti" echoes the title and matches the `629:15489` grammar). Red 1px border restored, so the destructive action is signalled again. **Label stayed `Surface 8` white rather than red — the better call:** the border carries the signal, white keeps the label legible.
      - 🔸 **ACCEPTED AS-IS (owner, do not "fix"):** the border inherited the original red **gradient** instead of a flat stop, so it fades along the top — measured against the `Surface 4` dialog: top `#660012` **1.22:1**, sides `#B3242F` 2.48:1, bottom `#FC474A` 4.75:1. At 1.22:1 the top edge is effectively invisible, so the outline reads as an open-topped bracket at the top-left curve. Flattening to one value would fix it (`#F94549` ≈ 4.7:1 all round, or `#B3242F` for an even quieter 2.48:1). **Owner declined 2026-07-29 — leave it.**
      - **Do NOT go further than that.** De-emphasize, don't obstruct — making cancellation hard to find on an auto-renewing subscription is exactly what consumer-protection bodies flag, and the site's own footer links Ditjen PKTN. One tap, plain label, no friction steps, no guilt copy. The retention win comes from the facts (poin stay safe, benefits run to 8 Jan), not from hiding the button.
      - Hygiene: title `967:19985` still named `Game Name`; the **gold "Nanti Lagi" frame is still named `Pilih pembayaran`** with a hidden `Lanjut Bayar` (`967:19990`) inside — now doubly wrong, since that frame holds the *safe* action. Stale hidden `Line 2` (`967:19982`) left at y136. No scrim/overlay frame (dialog only). Frame is fixed-size with **no auto-layout**, so any further copy change means re-heighting by hand again.
    - ✅ **POST-CANCEL STATE ALREADY EXISTS IN PRODUCTION** (owner screenshot, 2026-07-29) — **correction to my earlier note that it was missing; it's missing only in Figma, not in the product.** Live `/prime` account page shows: green toast **"✓ Langganan PRIME tidak akan diperpanjang otomatis"**, panel state **"Langganan Dibatalkan / Langganan berakhir 12 Nov 2025"**, and the CTA flips to **"Lanjut Berlangganan"**. Also live: a **"↺ Riwayat"** button in that panel = the existing entry point to subscription history, i.e. what `967:19481` maps onto. **Remaining Figma work is to draw these states, not to design them** — read them off production.
    - ❗ **STILL MISSING — row status variants.** All 10 rows are "Sukses". Landing FAQ 8 documents payment failure ("fasilitas PRIME kamu akan otomatis terhenti"), so **Gagal** is required and Pending likely. Match the Riwayat Poin ledger's status-colour convention.
    - ❗ **No desktop variant** — same gap pattern as Panduan Poin was.
    - ⚠️ **Placeholder data bugs (fix before handoff, a dev will copy these):** **"31/2/2025"** — 31 February, in all 10 rows · **"Rp 20,000"** but PRIME is **Rp 25.000**/bulan on the landing page and in prod · that amount also uses a **comma** thousands separator where the whole site uses a **period** (Rp 25.000 / Rp 12.000 / Rp 1.519) · date format **"1/3/2025"** is ambiguous (D/M vs M/D) and off-system — the site uses "8 Jan 2026" / "30 Des 2026", including this page's own header.
    - ⚠️ **No bottom tab bar** on this page, while the account page it's reached from has one (Beranda/Pesanan/Promo/Chat/Akun). Either add it or confirm this is a full-screen push.
    - Hygiene: page title, both header texts, the cancel-pill label and the "Tampilkan lebih banyak" label are all still named **`Game Name`** (`967:19663`, `967:19969`, `967:19970`, `967:19973`, `967:19490`); each row also carries a hidden `GoPay` text leftover (`967:19498` +9 more). Page name "PRIME History" undersells it — it's the management page, not just history.
  - ⚠️ **(b) Benefit-change notice for existing subscribers** (new 2026-07-29, from the retired blanket discount — see #16). Current subscribers bought "selalu ada diskon"; the account PRIME card is the natural in-product home for the notice, alongside the email. Copy must state what changed, when it takes effect, and what they get instead (3x poin) — and must land **before** the next renewal.
  - Sequencing: **(a) is nearly done** — page + cancel dialog both built; what's left is the **post-cancel state**, the dialog's missing 3 facts + button hierarchy, and row status variants. (b) is still gated on the grandfathering decision in #16. The two interact: someone cancelling right now may be cancelling *because* the discount was removed, so the cancel-dialog copy and the change notice should be written together.

### Support states
- [~] ~~18. First-time cashback explainer modal~~ — **DROPPED (owner, 2026-07-29): "my users don't read."** Consistent with the 07-22 Panduan Poin pivot (users bounce on info overload). **No new surface will be built.** Rationale, so this doesn't get reopened:
  - A first-*visit* modal was the weakest version — fires before the user has any poin, hits guests who can't earn, and its only action is a dead-end "OK".
  - Two better-timed moments were considered and also dropped: a first-earn line on the delivered sticky banner, and a first-redeem sheet. The first is pure education (fair to drop). The second looked like consent-for-an-irreversible-spend, **but the interface already shows those mechanics without prose:**
  - **"Poin dipakai lebih dulu, sisanya gabung metode lain"** → already communicated by the redeem-toggle ON state **re-rendering every method price to the remainder** (Rp 12.104 → 10.854). Per #6 that re-render *is* the key interaction — it shows instead of telling, which is exactly what a non-reading audience needs.
  - **"Beberapa metode nggak bisa pakai poin"** → belongs to #6 open item (a) *per-method disabled + reason* and #7 non-redeemable. A visual state, not copy.
  - **"Poin terpakai nggak bisa dibalikin"** → only becomes relevant at refund, which is #21 clawback.
  - ✅ **RESIDUAL CLOSED (owner, 2026-07-31) — absence IS the explanation, and #7 is right as built.** Owner: *"when a user finds there's no toggle, they'll think this game is just non-eligible for using poin."* Correct, and that inference is **true** — which is the actual test. Absence only fails when it produces a *false* belief ("my poin are gone", "it's broken"); here it produces an accurate one. A disabled toggle would be strictly worse: a look-but-can't-touch control on the checkout page at the moment of purchase, drawing attention to something unavailable. The one residual risk — over-generalising to "poin don't work anywhere" — is **already covered by Informasi Poin row 2, "bisa dipakai di hampir semua game"**, which sits in the wallet where users go to look at their poin. No checkout change needed.
- [x] 19. Error states set — ✅ **BUILT 2026-08-03 → `Poin — Error & Edge States (#19)` `1158:23243`**, page "RRQSHOP Rewards" @ x4726 y6100, 1333×884. A labelled **3-cell** reference board, each state drawn in its real surface rather than as loose components — a dev needs to see where the string lands, not a swatch. (Four were built; the saldo-berubah cell was cut on review — see the 🚫 entry below.)
  - **The pattern rule that came out of building it: a failed *action* gets a dialog; a changed *fact* gets an inline note — and a fact the user would infer correctly on their own gets nothing at all.** That third clause is what cut the fourth state. Only the redeem failure asks the user to choose, so only it is modal; do not promote the inline notes to dialogs, they would interrupt a purchase the user already decided on.
  - **1 · Event 2x berakhir mid-session** (`1158:23250`, member drawer). ✅ **RULE DECIDED (owner, 2026-08-03): re-fetch and explain — not hold-the-rate, not silent.** The card corrects itself on return; if the number changed between tap and confirm, the drawer's poin section carries **"Event 2x sudah berakhir, jadi poin dihitung dengan rate normal."** directly under the earn row, where the number it explains actually is. Hold-the-rate was rejected because it is a promise the UI cannot keep alone — the rate would have to be frozen per session server-side, and the locked law is that no number is client-held. Silent was rejected because it is the trust bug the item was raised for.
  - 🚫 **Saldo poin berubah — BUILT THEN CUT (owner, 2026-08-03). No UI. It is a dev rule, not a screen.** The behaviour still stands: **auto-correct, never block** — balance drops between page load and pay, the toggle silently re-renders to the real number, method prices re-render with it, redeem succeeds with whatever is actually there. What was cut is the explanatory note ("Saldo poin kamu berubah sejak halaman ini dibuka").
    - **Why it was right to cut:** auto-correcting produces a **true** belief — the toggle says 800 because the user has 800. That passes the same test #7 and #18 are decided on, and most users never registered the old number to begin with. It is also **not** the price-reversal case: that one reverses the user's own action with the total rising while they watched; here the correction lands before the user acts. *(Written as "the DANA case" before 2026-08-20 — that rule was retracted; the reversal shape is real, the DANA instance was not.)*
    - ⚠️ **The one condition that brings it back:** if the client re-fetches `/preview` **while the payment screen is already visible**, prices visibly jump (Rp 10.854 → 11.304) under the user's eyes — that *is* the price-reversal case and needs a word. If the fetch happens on entering the payment step, or only at submit, nothing needs saying. **Tech owns this choice**; design's answer follows it. The cut frame was `1158:23352` and can be rebuilt from the pattern in one pass.
  - **2 · Poin kedaluwarsa saat checkout** (`1158:23463`, payment step). Wallet expiry passes mid-checkout → balance is 0, so **the toggle card is removed entirely** and its slot states **"Poin kamu kedaluwarsa sebelum pembayaran selesai, jadi nggak bisa dipakai di pesanan ini."** Prices return to Rp 12.104 — and any method that had dropped out for failing the **remainder** minimum becomes available again. *(Read "DANA is enabled again" here before 2026-08-20; retracted — no method is blocked by poin.)* Silent removal was not an option: the total jumping back up with no explanation is exactly the DANA reversal from #6(d).
  - **3 · Gagal pakai poin** (`1158:23574`, dialog). Cloned from the `629:15489` chrome so it matches the app dialog system. Body leads with the reassurance, because "did my poin just disappear?" is the actual anxiety: **"Poin kamu nggak jadi kepakai di pesanan ini dan saldonya tetap utuh. Kamu bisa coba lagi, atau lanjut bayar tanpa poin."** Buttons **quiet "Lanjut Tanpa Poin"** (left) / **gold "Coba Lagi"** (right) — gold sits on the action the user was already trying to take, matching the `629:15489` and cancel-dialog precedent that gold is the constructive option.
  - **Copy convention:** every string names the cause before the consequence, and none of them apologise. All four are `Surface 7` against the surface they sit on (≥7:1) except the dialog body, which keeps the existing dialog styling.
  - ⚠️ **Trap, cost a round trip and will again:** a TEXT node set to `textAutoResize = 'HEIGHT'` does **not** re-measure when its width changes afterwards (e.g. via `layoutSizingHorizontal = 'FILL'`). Both notes kept `height: 16` while wrapping to two lines, so auto-layout allocated one line and the second line ran under the next row — it renders as a crop, not as an overflow. Fix is `layoutSizingVertical = 'HUG'` on the text once its width is final. Same family as the checklist's "FILL alone collapses a wrapping text to zero width", which also hit this build.
  - ⚠️ **Still owed by Tech, not design:** does `/preview` expose *why* a number changed (event ended vs balance changed vs expired), or does the client have to infer it by diffing? These four states each need a distinguishable signal, otherwise the front end will pick one generic string and the specificity here is wasted.
  - ⚠️ **Not drawn, lower priority:** network failure on `/preview` itself (poin section can't render a number at all) and the remainder-below-method-minimum case from #6(a), which shares state 3's mechanism but needs its own reason string.
- [ ] 20. Terms & Conditions page — NEW. Carries: the 8-bullet S&K + tier table dropped from the Panduan Poin drawer (product-group % framing, never rupiah amount-bands) · **which product groups cannot redeem** (a backend-managed category rule — never a payment-method exclusion; the DANA exclusion was retracted 2026-08-20) · **the reseller/pricing statement** the live T&C is missing (see [publisher-compliance-copy-rule.md](publisher-compliance-copy-rule.md) §3): "RRQSHOP adalah penyedia layanan top up pihak ketiga. Harga yang tercantum ditentukan sendiri oleh RRQSHOP dan tidak mewakili harga resmi dari penerbit game." · **and the PRIME benefit terms rewritten for the retired blanket discount** (see #16 — PRIME = poin multiplier + scoped Flash Sale/coupon discounts only; this is also where the grandfathering decision has to be written down).
- [ ] 21. Refund → clawback states — NEW, richer than assumed (backend): unspent → lot canceled (ledger shows reversal); spent → **clawback DEBT**: wallet shows outstanding debt, "redemption blocked until cleared" notice, future earns net against debt ("+1.000 → issued 0, debt −1.000" ledger rows).

### 🎨 Figma component set — Nominal Card (BUILT ✅ 2026-07-10)
[Nominal Card Mobile — Figma](https://www.figma.com/design/RWkIM1Q8o5pqt6eK3O8WBe/RRQ-Topup-2.5?node-id=529-895) (frame `529:895`, page "RRQ Topup 2.5")
- **24 variants**, properties: `tag: none|promo|flash` × `poinState: none|normal|event|prime` × `selected: false|true` — full 3×4×2 matrix, verified against the live file.
- Card 164px wide; `poinState=none` (non-eligible) = 105px tall (no pill), others 133px.
- Encodes all locked rules: pill "+N poin" · chip GREEN 2X (event) / GOLD 3X (PRIME) · glyph arrows green=event, gold=PRIME ("always boosted") · FLASH sold-out = revert to default (no extra state) · selected = yellow border.
- Multiplier chip label + all numbers = text properties (fed by CMS label + /preview at runtime).

### Event 2x — nominal card + strip rules (LOCKED 2026-07-10)
- **Nominal card (FINAL, revised):** **no 2X tag** — dropped as redundant. Events bind per game/product-group → whole sections boost together, so a per-card tag ×20 = noise (same reasoning that killed the per-card PRIME teaser). Scan-level announcement = **section-header chip**; per-card proof = **boosted pill** "◈↑ +70 ~~+35~~" (green up-arrow glyph variant — event-only asset, reused across pill/strip/wallet/chip; never outside active events). Numbers from /preview (events auto-pause on budget).
  - Division of labor locked: **tag slot = price benefits only** (PROMO gold / FLASH orange); **pill = poin**; purple never competes for the tag slot. Kills both collision QA cases (tag vs selected border, tag vs PROMO side-by-side).
  - **Granularity FINAL (2026-07-10 #2): events apply per GAME** (supersedes the per-item note). Whole game page boosts uniformly → all pills boosted together, no mixed state. **Section chip dropped; NO countdown** (stakeholder call 2026-07-10) — strip full-swap with plain copy: member = "◈↑ ⚡ Lagi event 2x poin — semua nominal ❯"; guest = "◈↑ Event 2x poin loh, login biar kebagian. [Login]". No-tag stands (uniform boost → per-card tag = noise ×20; strip owns scan-level). Urgency lever intentionally unused in V1; if event conversion underperforms, countdown is the first knob to revisit (soft alt: "waktu terbatas" in copy, no timer).
  - **Boosted pill FINAL (revised 2026-07-10):** multiplier chip REPLACES strikethrough — "◈↑ [2X] +70 poin" (chip carries the comparison; no struck old value). **Chip color = boost source:** GREEN = event, GOLD = PRIME (3X). **Glyph = THREE variants (final, owner decision 2026-07-10):** normal (no arrow) / event = GREEN arrow (temporary boost) / PRIME = GOLD arrow on all PRIME pills — deliberate "always boosted" feeling as a PRIME retention device (overrides the semantic arrow-means-temporary argument). Arrow color follows source law: green event, gold PRIME. **No PRIME+event state exists** (PRIME immune to events). Real combo = PRIME/member + payment additive → checkout-only: earn line re-fetches /preview on payment-method select, optional microcopy "termasuk ekstra dari [channel] ✓" (no percentages), no extra chip. "Max" celebration variant (sparkles/count-up) reserved for selected/detail; never loop animations.
  - **Pill copy includes the word "poin"** ("+35 poin") — owner decision: ID users need the currency named while the program is new (supersedes glyph-only rule on cards). i18n to "points" for MY/PH; revisit dropping the word post-adoption if width becomes an issue.
  - **Multiplier copy law:** all marketing surfaces say "2x / 3x poin" — percentages banned outside T&C/technical tables. Multiplier label = CMS display field per event (never client-computed from rate ratios — member/prime ratios can differ).
  - **Homepage tile badge → FINAL v2 (2026-07-10, implemented & approved):** three states, glyph 40px thick-outline brand style, **top-right (LOCKED — top-left rejected 3x: ML ID/MY flagship logos live top-left; right side only collides with lower-tier eFootball)**. Non-eligible = plain · Eligible = purple frame + plain glyph · Event = **dominant-green frame + event glyph with RESCALED green arrow, arrow anchored on the INNER side** (badge top-right → arrow bottom-LEFT of token, facing card center so it renders over the art, never clipped by the card edge; mirrored asset variant, arrow still points up). Rule: arrow always faces card interior. **No chip. No PRIME state on tiles** — tiles mark GAME properties (eligible/event); PRIME is a USER property applying to all eligible games equally, so a tile-level PRIME state would light every tile identically = zero information. Gold lives only at pill/strip/wallet level. (owner call: chip too convoluted; multiplier told by banner + game page). Rule enforced: one differentiator knob remaining (frame hue) → must be decisive binary purple/green, green clearly visible at the glyph corner — purple-green blend rejected for muddying the signal. Known-issue: top-right-logo arts (eFootball) collide if they become eligible → alt art request, not layout change. Guardrail: eligible % majority → drop eligible frame. Carousel owns event storytelling.
  - Rejected: 2X corner tag (redundant w/ chip+pill), purple card border (selected-state collision), watermark-as-signal.
- **Strip slot rule — never two banners:** one strip slot, content = f(audience, event). Guest state always owns the strip (conversion first): no event = "Top up dapat poin, belanja lebih hemat. [Login]"; during event = "⚡ Lagi event 2x poin — login biar kebagian. [Login]" — claim + requirement in the same sentence, so guests are never shown naked "2x" they can't earn. Member: default education → event swap. PRIME: flex → event chip.
- ~~Section-scoped events / countdown chip~~ — **SUPERSEDED**: events are game-level (uniform boost, no chip, no countdown). Kept for history only.
- **Guest pills show boosted potential (+70) undimmed** — backend designed /preview + potential_cashback as the guest conversion surface; the strip carries the condition, the pill carries the lure.

### Checkout drawer — REDEEM (Pakai Poin) state (2026-07-22, in progress)
- **Skipped state found:** drawer set `Konfirmasi Pesanan / Poin` (587:12570) has guest/member/prime/event/new-member — all EARN, no **redeem (pakai poin)** state.
- **Redeem layout (owner-approved):** payment box gets a line **"Pakai Poin −Rp N"** (PURPLE, below Promo) → Total Bayar = net. Poin section earn = on net (smaller number, per earn-base=net rule).
- **Sample built + refactored to auto-layout:** `839:12570` (member + pakai poin, page RRQSHOP Rewards). Payment box converted absolute→AL: unified `Surface 6` container (radius 4) + `Surface 1` details panel inset (dot pattern kept, radius 4) + rows (label/value HORIZONTAL space-between) + Total Bayar strip on the `Surface 6` bottom (flush, no gap). Owner fixes applied: radius 4 (not 10), pattern restored, total-bar unified (not separate block), spacing proportional.
- **Owner manual fixes (2026-07-22, final spec for the AL payment box):** total-bar row padding = **top/bottom 6** (not 13 — thinner bar, h≈34) · box (`Surface 6` container) = **full 343 wide, x0** filling the outer frame (no double-frame inset; the 4px detail inset comes from the box's own padding `[t4,r4,b0,l4]`) · details panel `Surface 1` = 335×~201, radius 4, padding 12, gap 12 · outer frame `839:12572` height ≈241, drawer height ≈590 (tightened) · disclaimer + buttons nudged up for tighter vertical rhythm. **Use these exact values when propagating to the drawer set.**
- **✅ Sample fully AL + prop (2026-07-22):** `839:12570` content wrapped in VERTICAL AL (bg abs · HeaderBar · Content[GameHeader row · divider · Poin section · Payment box AL · Disclaimer · ButtonsRow]) — reflows. Boolean prop **`Pakai Poin`** (default ON) bound to redeem-row visibility → toggle hides "Pakai Poin −Rp N" + payment box + drawer reflow automatically (tested OFF). This sample = the reference redeem drawer.
- ✅✅ **DONE 2026-08-03 — the drawer SET `587:12570` is now auto-layout and carries the redeem state. 9 variants.** All five originals were rebuilt on the sample's AL skeleton (the sample's `Content` frame was cloned into each, then each variant's own poin section swapped back in), so GameHeader / poin section / payment box / disclaimer / buttons all reflow. Old absolute nodes deleted.
  - **The `Pakai Poin` BOOLEAN was rejected — it ships an arithmetic bug.** A Figma boolean cannot be inverted, so one `Total Bayar` text node has to serve both states: with the redeem row hidden the total still read **Rp 147.705** under `Harga 154.955 − Promo 5.000`, a visible 2.250 mismatch that a dev would copy. Second variant property instead: **`pakaiPoin = false | true`**, giving `state`(5) × `pakaiPoin` — **9 variants** (guest has `false` only; a guest has no poin to spend).
  - `pakaiPoin=false` → PakaiPoin row hidden, **Total Rp 148.955**. `pakaiPoin=true` → row visible at **−Rp 1.250**, **Total Rp 147.705**.
  - **Earn numbers recomputed on the net amount** in every `true` variant, per the locked earn-base=net rule: member 1.489→**1.477** (upsell 3× = **4.431**) · prime 4.467→**4.431** (bonus +2.954) · event 2.978→**2.954** (bonus +1.477) · new-member 11.916→**11.816**.
  - **Two number bugs fixed while in there:** member's PRIME upsell read 4.468 and prime's earn read 4.468 where 3 × 1.489 = **4.467** (bonus 2.978). The 3× claim now holds exactly on both surfaces.
  - **Sizing fix that made the reflow work:** `Frame 115547` was a plain frame and `Frame 115551` was FIXED height 241, so hiding the redeem row left 29px of dead space. 115547 is now a pass-through VERTICAL auto-layout, both hug vertically → box 241 (redeem) / 210 (no redeem). Variant heights: guest 547 · member 561/590 · prime 526/555 · event 526/555 · new-member 539/568.
  - Set re-laid-out to `layoutWrap: WRAP`, `counterAxisSpacing: 60` → row 1 = the five `false` variants, row 2 = the four `true` ones. Set is 2528×1262.
  - New variant node IDs: member `1123:12586` · prime `1123:12685` · event `1123:12784` · new-member `1123:12888`.
  - ⚠️ The sample `839:12570` ("CONTOH / Drawer + Pakai Poin") is now **superseded** — it was the source of the skeleton and is safe to delete once someone has eyeballed the set.

### 🎨 Figma — drawer poin states BUILT (2026-07-10, co-built via Figma agent)
**Component set: `Konfirmasi Pesanan / Poin` (587:12570)** — variant property `state: guest | member | prime | event | new-member` (5 variants).
- **New-member rules (2026-07-10):** backend override — new-member % REPLACES entire pipeline (base/event/promo ignored). UI precedence `newMember > event`: never show event visuals (2x, "dari event") when override fires. Visual borrows event language (green = temporary boost — color law refined: green = temporary [event OR first-transaction bonus], gold = PRIME permanent). Sub-line: "Bonus member baru — transaksi pertamamu".
- **Guest nudge = DUAL-NUMBER, login-framed (final, owner 2026-07-10):** "Login sekarang dan klaim **+[base] poin** (Bonus **+[delta]** untuk member baru) di pesanan ini." + Login CTA. Base number purple, new-member delta green (temp-boost family). Serves both audiences honestly: existing member sees their real number, new guest sees the bonus on top; no misrouting to register. API: /preview (guest) returns base + new-member potential + eligibility flag.
- Celebration moment for first-transaction bonus lives on the SUCCESS page (not yet designed), drawer stays receipt-toned. Reference frame (not in set): `5 - REF Kitchen Sink` (583:12570) for the multi-source worst case. Glyph components: Default/Small (507:1468) · Small/PRIME (554:1663) · Small/Multiply (507:1467). Numbers = placeholders, production from /preview (per-source breakdown required).

### Checkout confirmation drawer — poin states (LOCKED 2026-07-10)
Structure REVISED (owner, 2026-07-10): **poin = its own section ABOVE the payment box** ([Figma 554:1317](https://www.figma.com/design/RWkIM1Q8o5pqt6eK3O8WBe/RRQ-Topup-2.5?node-id=554-1317)) — money box stays pure Rupiah (mixing poin into payment rows risks "does poin reduce my total" confusion). Drawer order: game header → poin section → payment box → disclaimer → buttons (reward before bill). Poin section border = state identity: purple member / gold PRIME / green event. All numbers from /preview.
- **Guest:** the poin section IS the login nudge (same slot, different content): "**+[N] poin nunggu kamu di transaksi ini — jangan hangus, login dulu**" + gold Login button.
- **Member:** section = row "Poin didapat ◈ +[N] poin" + PRIME nudge INSIDE the section (divider row, gold): "**Upgrade PRIME, poin transaksi ini langsung jadi +[3N]**" → PRIME landing. Side-by-side comparison in one box. Retires old "potongan Rp 22RB" discount nudge (framing conflicts with new positioning).
- **PRIME (final, owner-simplified):** single-row dark section, thin gold border — PRIME glyph + "Poin didapat" + GOLD number + outline **3X badge AFTER the number** + dim sub-line "**+[N] poin ekstra karena PRIME**" (Inter Regular 11, right-aligned — per-transaction subscription-ROI line, anti-churn). Affirmation slogan dropped (number > slogan). No upsell.
- **Sub-line grammar (REVISED — left-stack receipt layout):** section = two columns; LEFT = label + dim origin lines (Inter Regular 11) stacked below it, RIGHT = final total + chip, vertically centered. **One dim line per boost source**, pipeline order: multiplier (event 2x / PRIME) → payment channel → promo code. **Drawer has NO multiplier chips at all** (removed 2026-07-10): border + glyph + sub-line already carry source+multiplier — chip was a 4th/5th redundant signal. Chips remain a nominal-card-pill device only (comparison surface needs scan speed; the drawer is a receipt). Worst case 3 lines (member event+payment+promo); PRIME max 1 (payment only). Reference frame: `5 - REF Kitchen Sink` (583:12570).
- **API requirement:** /preview must return per-source breakdown (mirror lot `calc_breakdown`), not just the total — flag to backend.
- **Member + event (SIMPLIFIED, owner 2026-07-10):** green-bordered section, SINGLE row: "Poin didapat ◈↑ +[final] [2X chip]" — chip-after-number pattern (same grammar as PRIME 3X badge; chip color = source). Event breakdown row AND PRIME upsell both removed — during events the boosted number is the sole star; upsell lives only in the normal member state. PRIME during event: unchanged (immune).
- Optional small print: "poin masuk setelah item terkirim" (delivered-to-earn expectation).

### New from backend spec (2026-07-10)
- [~] 25. New-member first-topup bonus moment — **drawer state DONE** ✅ (`state=new-member` + dual-number guest nudge); still open: success-page celebration, wallet entry. **Rule 1.10b (2026-07-21): suppress this moment for already-PRIME users** (new-member state / dual-number nudge must NOT fire if user was PRIME before first txn — dev state-gating, no new frame).
- [x] 26. PRIME coupon-downgrade warning ✅ — **popup at "Cek" moment** (owner-refined 629:15489): generic "?" confirmation dialog (matches app dialog system), title "Konfirmasi", body explains fewer poin vs PRIME benefit, **"Hapus Kode"** (clears input) / **"Ya, Tetap Pakai"** (gold) → purple banner. Supersedes 3-step drawer mock + amber banner.
- [x] 27. Coins-only checkout flow — **payment-step state BUILT** ✅ (610:12699: method section hidden + subtitle "Pesanan ini dibayar sepenuhnya pakai poin"). ✅✅ **SUCCESS PAGE BUILT 2026-08-03 → `1138:22227` "Pesanan Selesai - Bayar Penuh Pakai Poin (coins-only)"**, page "RRQSHOP Rewards" @ x4726 y3734, 375×2069. Cloned from the current `Pesanan Selesai` (`650:3049` — the one carrying the rebranded `Header / M`, not the older `648:14752`).
  - **Metode Pembayaran = "Poin RRQSHOP Rewards".** A receipt has to say how it was paid; the checkout hides the method list, but the receipt cannot just leave the row blank.
  - **PRODUK panel goes from 2 rows to 4:** ID User · **Harga Layanan Rp 2.659** · **Pakai Poin −Rp 2.659** (purple `Rewards 4`, both label and value) · **Total Bayar Rp 0**. The old single "Total Rp 2.659" row was relabelled `Harga Layanan` so the panel reads in the same grammar as the confirmation drawer the user just came from — "Total" sitting directly above "Total Bayar" would have been two different meanings of the same word. ⚠️ **If that's right here it is probably right on the normal receipt too** — the base frame still says "Total". One-line fix, not done, because it touches a non-poin surface.
  - **Sticky banner restated, not deleted.** Earn base = net, net is Rp 0, so **a coins-only order earns nothing** and "Kamu dapat N poin dari pesanan ini 🎉" would be a lie. The slot now reads **"Pesanan ini dibayar penuh pakai 2.659 poin"** (Barlow Medium 14 `Rewards 4`, "2.659 poin" Bold, token glyph, re-centred at x45). Keeping the poin surface present and truthful beats deleting it — deleting reads as a rendering bug on the one page where the user just spent their whole balance. No 🎉: nothing was earned.
  - Panel chain grown by 64 (2 rows × 20 + 2 gaps × 12); footer / banner / bottom bar / nav shifted +64; frame 2005 → 2069. Verified on re-render — no corner distortion on the vector panel backgrounds.
  - 🚨 **PRODUCT DECISION THIS SURFACES, not design's to make: paying entirely with poin earns zero poin, and therefore does not extend `expires_at` either** (expiry resets on earn *activation*; redemption never extends — locked rule). So the most engaged redeemer, the one who spends their whole balance, is the one user who gets no expiry reset. Options: floor the earn base at the pre-redemption amount for coins-only, earn on the gross for every order, or accept it. Needs PM/Tech, and the answer changes this page's copy.
  - ❓ **Left deliberately unwritten: an explicit "nggak ada poin masuk" line.** Absence plus the restated banner probably carries it, and the owner's standing call is that users don't read (see #18). If it's wanted, it belongs as a muted sub-line under Total Bayar in the PRODUK panel, not in the banner — the banner strip is only ~40px before the Beli Lagi bar.
- [x] ~~28. Pending-lot stale/canceled state~~ — **CLOSED 2026-07-31: no new design needed, both states already exist.** The ledger sign convention already locks **Canceled = "0" `Surface 6`** and **Pending = amount purple + "Pending • [date]"**, the desktop poin page already ships a live "Pesanan Dibatalkan • [date]" row, and #15's card set already handles `Dibatalkan`. What was missing was a written rule, not a screen:
  - **Dev rule:** a pending lot that cancels **updates in place** — same row, amount → **"0" `Surface 6`**, subtitle → **"Pesanan Dibatalkan • [date]"**. Do **not** delete the row and do **not** append a second one. Keeping it is what makes the balance arithmetic explainable ("I was going to get 240, the order was canceled"); deleting it makes poin appear to vanish.
  - ⚠️ **Rejected the ">30d" framing.** A poin lot must not carry its own staleness clock — poin are earned **on delivery**, so a lot is pending purely because its *order* is in progress. If an order stalls, that's an order-system concern; the ledger just mirrors whatever the order does (`Dibatalkan` → canceled row). Inventing a parallel 30-day poin timeout would let the ledger and the order list disagree. **For Tech, not design: does the order system actually time out a stalled order, and does it land on `Dibatalkan` or `Terkendala`?** The ledger follows that answer; it doesn't set it.
  - Consistent with the #14 debt-state precedent (owner: rare edge case → no special design beyond the existing states).
  - ✅ **RESOLVED (owner, 2026-07-31): the big SALDO number = `available` ONLY.** Pending is excluded from the headline balance and stays visible in the ledger where it already is. **This is a locked data rule, not a styling choice — it protects the 1:1 peg**: if pending were included, "Setara dengan Rp N" would overstate what the user can actually spend, and a later cancellation would silently drop the balance. Applies to every surface showing a balance: wallet SALDO card, the account **Poin** stat cell (#13), the desktop poin card (`972:9896`), and the E2 email's `{{poin_balance}}` merge field. **Dev: bind to `balance.available`, never `available + pending`.**
- [ ] 29. PRIME bonus lots in wallet — welcome bonus + renewal bonus entries (born active, no delivery wait).

### Events & coupons
- [~] 22. Cashback Event landing / listing page — **RECOMMEND: do NOT build as a standalone page. Reframe as a section inside Promo, pending one ops answer.** (2026-07-31)
  - ⚠️ **It would be the THIRD promotional surface.** The 07-31 nav decision keeps **Promo** as a bottom tab *and* adds **Rewards** as the centre tab. A separate "Cashback Events" page competes with both — events are promos, and events are about poin. Two surfaces is already the ceiling.
  - **The decision point is already covered, and that's where events actually matter.** Built: nominal card set `529:895` `poinState: event` → **green 2X chip** + green glyph arrows · **strip event swap** ("⚡ Lagi event 2x poin — login biar kebagian") · homepage tile badge 3-state. Colour law (green = temporary boost) already applied throughout. A listing page serves **discovery**, not the moment of choosing what to top up — strictly lower value than what exists.
  - 🚧 **BLOCKING QUESTION (ops/CMS, not design): how many cashback events run concurrently, and are they already listed on the existing `/promo` page?** If events are just promos with a poin flavour → **#22 is a filter or section inside Promo**, a day's work. If a dozen run at once and they're operationally distinct → it's a page. Nothing should be drawn before this is answered; guessing wrong builds a surface that competes with Promo.
  - **If it does get built, countdown is the expensive part:** needs a real `ends_at` from the API (never client-computed, same law as the multiplier labels), WIB timezone, and a defined behaviour at zero. Budget it separately from the listing.
  - 🚨 **INDEPENDENT GAP, worth logging now because it exists whether or not #22 is ever built — event expiry mid-session.** The card and strip say **2x**; the event ends; the user checks out 30 seconds later; `/preview` correctly returns **1x**. The confirmation drawer then shows a different number from the card the user just tapped. That is a trust problem at the worst possible moment. Belongs in **#5/#6 checkout + #19 error states**, not here. Needs a rule: does the card re-fetch, does the drawer explain the change, or is the rate held for the session?
- [~] 23. Coupon entry at checkout — **2 banner states DESIGNED** (owner, frames 115506/116002): GREEN + discount icon = "berpotensi hemat Rp X" (money benefit) · PURPLE + boosted token = "berpotensi dapat bonus X poin" (cashback code). Icon = disambiguator, color reinforces. **Combo BUILT** ✅ (635:12570: green above purple, stacked, pipeline order) · **PRIME warning = POPUP at "Cek"** ✅ (owner-refined 629:15489): "?" confirmation dialog matching app system, "Konfirmasi" + trade-off body, "Hapus Kode" / "Ya, Tetap Pakai" (gold) → insist shows purple banner. Amber inline banner killed. Green promo banner toned down to match purple's visual weight (neither shouts louder). Drawer stays clean (no trade-off step there). ~~⚠️ backend-blocked~~ **UNBLOCKED 2026-07-21** — backend adding `promo_code.cashback_rate` write-path; approved V1. Built banners stay valid.
- [ ] 24. My Coupons page (account) — NEW (available / used / expired coupons). **UNBLOCKED 2026-07-21** — backend adding coupon cashback write-path; approved for V1, proceed. (Was blocked on `promo_code.cashback_rate` write path.)
  - ⚠️ **Attempted 2026-07-31 and REVERTED by owner.** Desktop `Kupon Saya - D` + `/ Empty` were built by cloning the PRIME Voucher page into the account shell, then **deleted — owner does not want this area changed.** Do not rebuild without an explicit go-ahead.
  - Useful findings that survive the revert, for whoever picks this up: the PRIME Voucher page (`972:13189`) already solves the layout — 2-col grid in the 786 right panel, available cards then dimmed ones, footer "Semua voucher sudah ditampilkan", no tabs or section headers. Card state is expressed purely by dropping the inner frame to **opacity 0.6**. `Empty State / M` → `state=kupon-kosong` still has no home.

---

## Phase 2 — Shared components (6)
- [ ] C1. Header points balance chip (global) — **⚠️ SUPERSEDED by the 2026-07-31 nav decision below.** The balance now ships in the **sandwich menu's poin block** (`Sandwich Menu / M`, logged-in variant). Owner picks one and closes this: **(a)** accept the sandwich as C1's home — 2 taps, but built and done; **(b)** put the balance on the Rewards bottom tab — 1 tap, most glanceable, needs a badge design; **(c)** build it into the header gap the rebrand freed up (135px, consequence 3b). Not all three.

- **🔒 GLOBAL NAV DECISION — LOCKED (owner, 2026-07-31).** Resolves where the PRIME button lives now that the header PRIME circle is retired. **Rewards moves out of the header into the bottom tab bar; PRIME becomes a state-conditional header button that appears ONLY where the pitch is true.** Chat leaves the bottom bar into the sandwich menu.

  | State | Header | Bottom bar | PRIME entry |
  |---|---|---|---|
  | **Guest** | logo · search · sandwich | Beranda · Pesanan · **Rewards** · Promo · Login | inside sandwich menu |
  | **Member (non-PRIME)** | logo · search · **PRIME** · sandwich | Beranda · Pesanan · **Rewards** · Promo · Akun | header gold circle |
  | **PRIME** | logo · search · sandwich | Beranda · Pesanan · **Rewards** · Promo · Akun | none (already subscribed) |

  - **Rewards sits at position 3 of 5 — the centre slot, deliberately** (most prominent position in a bottom bar). Guest taps Rewards → Rewards landing (guest variant, Login CTA); member taps Rewards → wallet.
  - **No header Rewards circle in ANY state.** One entry per program — the 30px circle in `740:18931` (instance `740:18915`) is removed, not moved. Two permanent entries would split tap data and re-clutter the header we just cleared.
  - **Why PRIME is state-conditional, not permanent:** since the blanket discount was retired (07-29), PRIME is no longer a parallel program — it is an accelerator on Rewards (3X poin). A guest can't earn poin, so a guest PRIME button can only lead to a login wall (confirmed on prod: the current gold "P" doesn't navigate). A PRIME member has nothing left to buy. Only the logged-in non-PRIME state has a true pitch, so only that state gets the slot.
  - **Chat → sandwich:** support was already triplicated (sandwich "Butuh Bantuan?" WhatsApp · footer WhatsApp+email · "Pesanan Bermasalah?" band). Chat tab was the 4th door. ⚠️ **Pull Chat-tab usage before shipping** — RRQ MinBot is still self-labelled Eksperimental, and "top up belum masuk" is the #1 user anxiety. Low volume → move; real volume → reconsider.
  - **✅ COMPONENTS BUILT (2026-07-31), page "RRQSHOP Rewards" `362:12714`:**

    | Component | Node | Structure |
    |---|---|---|
    | **`Header / M`** | `1014:21813` | 375×106. **One component**, boolean prop **`Show PRIME`**. guest→false · member→true · prime→false |
    | **`Header / D`** | `1020:22750` | 1440×64. Variant `state = guest \| member \| prime` (all three genuinely differ — the account tab changes too) |
    | **`Bottom Bar / M`** | `1026:22496` | 375×76. Variant `state = guest \| logged-in` (member and prime are identical, so they share one) |
    | `Header Button / PRIME` | `1012:21825` | 30px `Surface 2` circle + 20px gold P monogram, rebuilt from prod `ico-prime-initial.png` |
    | `Logo / RRQSHOP` | `1017:21865` | 65.33×28 rebrand logo, instanced into every header |
    | **`Sandwich Menu / M`** | `1036:22248` | 271w, opened state. Variant `state = guest \| logged-in`. guest `1033:22285` 350h (Masuk · Negara & Bahasa · Blog · FAQ · Butuh Bantuan) · logged-in `1034:22238` 424h (**poin balance** · Negara & Bahasa · MinBot · Blog · FAQ · Butuh Bantuan) |
    | `Header Menu / D / Rewards` | `1019:22165` | **Local** desktop Rewards tab (Active/Inactive) — see blocker below |
    | Rewards tab, mobile | `1025:12597` / `1025:12570` | Real `Property 2 = Rewards` variants added to the **local** set `92:9393` |

    - **No redundant variants.** Mobile header guest≡prime, bottom-bar member≡prime and sandwich member≡prime were all collapsed — the mobile header has no account control, and neither the bar nor the menu changes on subscription. Desktop keeps all three because its account tab flips Login/Akun independently of the PRIME pill.
    - **Layout contract:** right-side controls in a hug auto-layout, 8px gap, pinned right (constraints MAX) — Header/M 139w↔101w with right edge always x359; Header/D 228w↔134w with right edge always x1320. Desktop `Nav` is a hug AL, 375w, centred. Bottom-bar `Tabs` is 375w with every tab `FILL` so they distribute evenly at any width — **`clipsContent` must stay `false`** or the active-tab caret (which overflows above y0) gets cut.
    - **Sandwich menu specifics:**
      - **Poin block** sits in the identity slot above the divider — the same slot guest uses for the Masuk prompt. Token + "12.000 poin" + "RRQSHOP Rewards" + chevron → wallet. Balance is placeholder; wire to `/balance`.
      - **`Poin Section BG` `1043:22261`** — `Rewards Surface 1` band (the Rewards landing panel colour), 267×59 at (2,2), top corners 12 / bottom square so it butts into the divider, layered above `Panel Fill` and below content, constraints STRETCH. **logged-in only.** Makes the balance read as a Rewards surface rather than a menu row.
      - **Every row has a chevron** except Negara & Bahasa, which keeps its `Ubah` pill. All chevrons sit at x219/y16 (constraints MAX) so they form one vertical line at 24px from row top whether the row is 49h or 62h.
      - **Icon convention (don't "unify" this):** generic actions get a grey ring + monochrome line glyph (Blog, FAQ); brand marks go bare and full-colour (Indonesia flag, WhatsApp, MinBot mascot at 28.8 to match the flag's optical size).
      - **Rebuilt from the base for reflow:** the panel background was two VECTOR nodes → now real rectangles (`Panel Fill` / `Panel Border`, STRETCH) so the panel can grow 350→424 without distorting the 12px corners; the row list `Frame 115968` was absolute-positioned → now a VERTICAL auto-layout, 12px gap, rows FILL, which is what lets MinBot slot in at position 2.

  - **🎨 REBRAND LOGO SWEEP (2026-07-31).** The vector is in; propagating it is partly blocked.
    - **✅ 9 legacy pages fixed with one edit.** The 38 `RRQ Topup Logo` usages resolve to **6 header masters**, not 38 nodes. `740:18931` (local, editable) now carries a `Logo / RRQSHOP` instance → Akun Regular, Akun PRIME, Point Detail, Riwayat Poin (Empty + Debt), Riwayat Pesanan and others all updated at once. 107 → 67.75 wide at x20 y65.
    - 🚧 **16 instances are library-locked.** Masters `767:4235` (8 uses) · `1:917` (3) · `1:844` (3) · `99:10220` (1) · `767:4194` (1) all live in **RRQ Topup 2.0** and are read-only from this file. ⚠️ **Note the framing:** these are not "PNG logos" — they are the **pre-rebrand TOPUP vector lockup**, so swapping them is a rebrand of existing mocks, not a format fix.
    - **📮 CONSOLIDATED ASK FOR THE LIBRARY OWNER** — two unrelated items now wait on the same person, send as one request: **(1)** add `Property 2 = Rewards` (and `Login`) to the nav tab set `99:9986`; **(2)** replace the legacy logo in the 5 header masters above.
    - **✉️ E2 email — SVG-first with a PNG fallback (owner call, 2026-07-31), and the header bar flipped to dark.** Served through `<picture>`: `<source type="image/svg+xml">` → `logo-rrqshop-email.svg`, with `logo-rrqshop-email@2x.png` (146×64, shown 73×32) as the `<img>` fallback. Verified rendering: Chromium picks the SVG, 73 × 30.2, aspect 2.4195.
      1. **The PNG fallback is not optional.** Owner chose to ship the vector; `<picture>` honours that where it works. But **Gmail, every Outlook and Yahoo strip or fail SVG in image tags** — they ignore `<source>` and render the `<img>`, so the PNG is what most subscribers actually see. Removing it ships a blank logo to the majority of the list.
      1b. **`logo-rrqshop-email.svg` is a TRIMMED copy, not the brand file.** Same paths, `viewBox` cropped to the real ink box `32.27 97.99 1374.29 568.01` (measured via `getBBox`). The brand original is a 1418×743 canvas with ~98px top / 74px bottom / 32px left padding baked in — pointed at an `<img>` untrimmed it renders small and off-centre. Re-cut the trim if brand reissues the SVG.
      2. **The header bar went `White` → `Surface 5`.** The bar was white *because the old wordmark had near-black "RRQ"*. The rebrand vector has **white `#FEFEFE` "RRQ"** — it is a dark-background lockup and would have vanished on white. Sandwich is now dark → purple → white footer. **If brand supplies a light-background lockup, revert the bar and re-export on white — do not recolour the artwork to fit the bar.**
      3. **The PNG is deliberately NOT transparent** — `Surface 5` is baked in so it composites without relying on alpha (old Outlook handles PNG alpha badly). **Bar hex and PNG backing must stay identical.**
      - ⚠️ **Still open:** the email's old logo was aspect **4.4** (a single-line horizontal wordmark); the rebrand lockup is **2.42** (coin + stacked RRQ/SHOP). It now displays 73×32 instead of 132×30, so the bar reads lighter. If the email wants its previous horizontal weight, brand needs to supply a **horizontal RRQSHOP lockup** — that variant does not exist in the SVG.

  - **BUILD CONSEQUENCES:**
    1. ~~Header `740:18931` needs auto-layout or 3 variants~~ — **DONE**, see the table above. The AL reflow is verified: toggling `Show PRIME` moves the cluster 139→101 with the right edge fixed.
    2. 🚧 **BLOCKER — the desktop nav tab set is a published library component.** `Header Menu` `99:9986` lives in the **RRQ Topup 2.0** library; `appendChild` returns `Cannot move node. New parent is a internal, read-only node`. The Rewards destination could NOT be added there, so desktop uses the local `Header Menu / D / Rewards` as a stand-in. **The library owner must add `Property 2 = Rewards`** (and ideally `Login`, currently faked as an Akun instance with an overridden label on both breakpoints). Mobile was unaffected — `92:9393` is local, so mobile got a real variant.
    2b. 🚨 **REGRESSION AGAINST THE DECISION — guest has NO route to PRIME.** The locked table says the guest PRIME entry lives in the sandwich menu, but the menu contents specified on 07-31 (both variants) contain no PRIME row. Header: removed. Bottom bar: never had it. Sandwich: not there. A guest's only path to a paid product is now a direct `/prime` link from ads or SEO. Either add a PRIME row at the top of the guest menu (gold, separated from the utility group — consequence 7) or accept ads/SEO-only acquisition as a deliberate call. **Also: guest gets no MinBot row and Chat has left the bottom bar, so guests lose in-app chat entirely** and fall back to the WhatsApp CS link.
    3. **C1 — likely ANSWERED by the sandwich menu.** The logged-in menu's top block now carries the balance (token + "12.000 poin" + program name + chevron → wallet), so the header chip has a home without touching the header. Note it's a 2-tap surface (open menu → read) versus 1 tap for the Rewards bottom tab; if glanceability matters, the balance belongs on the tab instead. Owner: close C1 as "balance lives in the sandwich top block", or spend the freed header gap below.
    3b. **Header gap from the rebrand** (see also above). The RRQSHOP logo is 65px wide vs the old 107, so the mobile header now has **135px of free space** between logo (ends x85) and the Actions cluster (starts x220 in the member state) — where it previously had 93. A balance chip (~75px) fits again, even in the busiest state. Kill C1, or build it into that gap.
    4. ~~The RRQSHOP logo is a raster image fill~~ — **DONE (2026-07-31): brand supplied `Logo RRQSHOP Vector.svg` (repo root), ratio revised to match the original lockup. Now a vector group inside `Logo / RRQSHOP` `1017:21865`, final 67.75 × 28** (old raster box was 65.33 × 28 — 2.4px wider, undistorted). Header chrome is fully vector. Mobile logo at x20 y65 ends at x87.7; desktop at x120 y18. Mobile free gap to the Actions cluster is **132.3px** — still ample for the C1 chip in consequence 3.
       - ⚠️ **RE-IMPORT TRAP, read before touching this asset.** The SVG canvas is `1418×743` (aspect 1.909) but the **artwork inside is `1374×568` (aspect 2.420)** — there is ~98px top / 74px bottom / 32px left padding baked into the file. Scale to the **ink**, not the frame, or the logo lands ~23% undersized and sits low in its box.
       - ⚠️ **Worse trap: SVG imports carry SCALE constraints on every child.** Any `frame.resize()` or reparenting into a differently-sized container scales those children **non-uniformly** and silently distorts the logo — this stretched the aspect from 2.42 to 3.05 and turned the coin into an ellipse twice before it was caught. **The only safe recipe:** import → `figma.group(children, PAGE)` (tight bounds, on the page, never into the component) → `group.rescale(28 / group.height)` → resize the component to the group **first** → `appendChild`. Verify the aspect is unchanged end-to-end before trusting it; the size readout looks correct even when the artwork is distorted.
    5. **The Rewards tab icon is a filled purple token among monochrome line icons.** Deliberate for now (it's the program glyph and the habit we're building), but if it should sit inside the icon system, someone needs a line-art token for nav use only.
    6. ~~Motion handoff needs re-authoring~~ — **DONE (2026-07-31), re-targeted to the Rewards NAV TAB on both breakpoints** → [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md). The pop + facet rake survive unchanged; the 30px circle chrome is retired with the header button. Token 20px→24px, tap feedback narrowed to the token only, and **four new rules** the header button never needed: (1.1) never animate while the tab is Active — gold caret + gold label already own that state; (1.2) **arriving on the Rewards route counts as discovery**, not just tapping the tab, since the sandwich poin block / wallet / deep links all reach it; (1.3) **only the nav tab animates** — the token now renders in up to 4 places at once (tab, sandwich poin block, wallet SALDO, nominal pills) and glinting them together reads as a rendering bug, so the token component ships inert; (1.4) glint budget **widened 15s→30s and capped 15→8** because a fixed bottom bar is permanently on-screen and the old cadence spent the whole lifetime budget in under 4 minutes. Desktop also gains an optional pre-discovery hover glint. Also fixed: the old spec's `aria-label` suffix was English ("new") in an Indonesian UI → "baru".
    7. **If a PRIME row is added (2b), it must not read as utility.** The rest of the menu is support/settings — Negara & Bahasa · MinBot · Blog · FAQ · Butuh Bantuan. PRIME goes at the TOP, gold, visually separated from the utility group. Buried between FAQ and Butuh Bantuan is worse than nothing.
    9. **`Cek Pesanan Manual` was dropped** from the menu in the 07-31 spec — it exists in the live sandwich today but appears in neither new variant. Guest order-tracking is the use case (guests have no account to check orders from), so confirm this is intentional and not an omission. If it stays cut, the "Pesanan Bermasalah?" band and the Pesanan tab are the only remaining routes.
    8. **Watch the Promo / Rewards adjacency** in the bottom bar (Promo = vouchers + discounts; Rewards = poin balance). The green-vs-purple disambiguation already covers the semantics; the two tab icons must be clearly distinct — as built, Promo is a monochrome discount badge and Rewards is the purple token, so they read apart.

  - **⚠️ MOTION SPEC IS NOW ORPHANED.** [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md) specs a **30px `Surface 2` circle + 1px `Surface 5` ring + inner shadow, glint clipped by `overflow:hidden` on a 20px token** — a header-button spec. The Rewards entry is now an unboxed ~24px bottom-tab icon with a label. **Token pop + facet rake survive; the container spec and its CSS do not.** Also decide whether first-run attract fires on a tab at all — it competes with the tab bar's own selected/active state. Re-author before FE handoff; do not ship the doc as-is.

  - **Motion spec — "First-Light + Facet Catch"** (motion judge-panel, 2026-07-22) — **written for the retired header circle; see the orphan warning above.** Interactive demo: https://claude.ai/code/artifact/2d8bc33e-436d-4378-bb75-2a0ca867435d
    - **Principle:** all energy in a <1.8s intro; steady-state provably free (no timer/RAF/canvas). Retire permanently on first tap (discovery = completion). Purple/lilac/white ONLY (never gold = PRIME).
    - **FE HANDOFF DOC:** [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md) — production CSS + React component/hook (gating + decay + reduced-motion) + token SVG + backend flag + QA checklist. Give this + the artifact URL to the front-end dev.
    - **States (owner-refined 2026-07-22, artifact = source of truth):** first-run attract (**token-only pop 1→1.38**, container static → **soft continuous facet rake ×2** (constant-velocity, no dwell), ≈2.9s once — premium pass 2026-07-22) · idle glint (facet-only, low amplitude, single-shot ~15s, sessions 2–3, cap ~15) · resting (plain static button, no glow) · tap (press 0.90 → spring 1.05) · reduced-motion (static lilac dot). **Everything stays INSIDE the button circle.**
    - **Owner revisions 2026-07-22:** (1) removed ring ripples that expand outside the button (sonar ping + tap ripple); (2) removed the amethyst halo glow entirely ("agak cheap"); (3) container matched to Figma exactly — 30px circle `Surface 2` + 1px `Surface 5` ring + inner shadow white 8% (offset y2, radius 2), token 20px. NOTE: these refinements live in the CSS artifact only — **not yet implemented to Figma** (owner: hold Figma). Figma storyboard 751:16702 still shows the earlier version.
    - **Decay:** `rewardsDiscovered` flag on USER PROFILE (not just localStorage). Session 4+ silent even if untapped. Tap retires all.
    - **Build (dev):** CSS keyframes + ~40-line React hook (NOT Lottie/Rive — they hold a surface at idle). Glint = gradient clipped by `overflow:hidden`, NOT SVG mask (smears on low-end Android WebView). `prefers-reduced-motion` → static lilac dot + `aria-label "…, new"`. Figma = storyboard/handoff only (can't do particles).
    - **Figma:** no motion artifact (storyboard 751:16702 was built then DELETED 2026-07-22 — it was an early version and would mislead the dev). Motion lives ONLY in the CSS artifact + handoff doc, which are the source of truth. Do not re-add motion to Figma unless asked.
- [x] C2. Eligibility badge → **FINAL v2** ✅ (2026-07-10): **rare-card frame + corner glyph, one integrated system** — thin purple holo-gradient frame around the whole tile (gacha "rarity" language) + Rewards diamond glyph seated ON the frame's top-right corner (names the frame: Rewards, not selected-state). Shown ONLY on eligible games; data-driven from rate-row API.
  - Why it survived review: immune to long titles (never touches name bar — "Umamusume Pretty Derby" test), zero art occlusion beyond the corner glyph, glyph disambiguates frame from border-state, no PROMO/FLASH collision (those live only on nominal cards, different page).
  - **Word hierarchy (intentional, don't "unify"):** game tile = frame+glyph (visual, no word); product/nominal level = "◈ +35" pill; education copy = "poin" (unit); strip = program context.
  - Rejected along the way: REWARDS sticker tag (superseded by frame system), glyph-only top-left (covers game logos), bottom pill (flag collision), name-bar integration (breaks on long titles), flat purple border alone (ambiguous with selection).
  - Side note for content team: tile titles like "Top Up Mobile Legends Termur.." truncate — title naming hygiene, independent of badge.
- [ ] C3. Earn-preview element (live-linked to redeem toggle + effective rate, reused)
- [x] C4. Empty states ✅ **CLOSED 2026-07-31 — but the brief was partly wrong, and the useful output is a page, not three components.**
  - **What the brief asked for vs what's real.** C4 listed "no points yet / all expired / no coupons". Checked against the file: **"no points yet" already exists** — `Riwayat Poin / Empty` `706:12570` shows balance `0` / "Setara dengan Rp 0" plus "Top up game favoritmu untuk mulai kumpulin poin." and an empty ledger. A second component saying the same thing 200px lower was pure duplication. And **"all expired" is not an empty state at all** — balance is 0 but the ledger is *full* of expiry rows, so it needs a page state, not an empty block. Both poin variants were built, tested against a real surface, and deleted.
  - ✅ **`Riwayat Poin / Hangus` `1127:22108`** (page "RRQSHOP Rewards", beside `706:12570`) — **the actual gap.** Third sibling to the existing `Empty` and `Debt` wallet states. Balance card at `0`, sub-line replaced with "Semua poin kamu sudah kedaluwarsa. Top up lagi buat kumpulin poin baru.", and the ledger populated with three real `Poin kedaluwarsa` rows (−4.969 / −240 / −28) cloned from the production wallet row. Panel grown 185→273 to fit; corner rivets repositioned after the resize.
  - ✅ **`Empty State / M` `1120:22067`** — trimmed to the single variant with a real consumer: `kupon-kosong` "Belum ada kupon" / "Kupon dari event dan promo bakal muncul di sini." ⚠️ Ahead of its page — **#24 My Coupons doesn't exist yet.**
  - **Pattern note for whoever builds #24:** the in-place empty state (`706:12570`) is a 40px grey glyph + **one** 12px `Surface 7` line. `kupon-kosong` uses glyph + primary 14px `Surface 8` + support 12px `Surface 7` — the file's existing 14/12 pair, no new scale. Glyph is desaturated via luminance-preserving greyscale (0.299/0.587/0.114) compressed to 0.25–0.85 so it matches the original grey clock's weight — **purple stays reserved for surfaces where poin actually exist.**
  - 🐛 **Fixed en route:** the "kadaluwarsa" misspelling the to-do flagged is on the **mobile** wallet (`695:17649`, "Poin kadaluwarsa pada 30 Des 2026"), not the desktop page — corrected to "kedaluwarsa".
- [x] C5. Event visual system ✅ — boosted pill (green arrow glyph + chip in nominal set), homepage tile event state (green frame + rescaled arrow), strip event swap. Countdown DROPPED (stakeholder). Event landing page (#22) still open.
- [ ] C6. Coupon card / token (list + checkout apply state) — ⚠️ **COUPON AREA IS PARKED (owner, 2026-07-31).** A `Coupon Card / D` set was built on 07-31 and then **deleted at owner's request**; nothing remains on canvas. Do not rebuild without an explicit go-ahead. The PRIME Voucher page `972:13189` is untouched and unaffected.
  - **Findings preserved for whoever picks this up:**
    - The precedent already exists — `972:13189` has the ticket-shaped card with the perforated edge, and expresses disabled state purely by dropping the inner frame to **opacity 0.6**. Extend that; don't invent a card.
    - **No status badges.** Owner: *"no need for Terpakai and Kedaluwarsa — it looks like a button. The disable state is enough."* Dimming carries the state; the date line carries the distinction ("Dipakai 3 Des 2025" vs "Berakhir 10 Jan 2025").
    - ⚠️ **Trap when cloning these cards:** on a disabled card the button frame `Frame 115681` is already `visible:false`, **but its label text ("Pakai") is not** — it sits loose and stays visible. Removing a badge alone leaves a floating word with no button. Hide the text too.
    - **"Checkout apply state" from the original brief is already covered** by #23's coupon banners (`635:12570`, green/purple/combo, DONE) plus the PRIME-coupon popup (`629:15489`). No new checkout artefact needed — don't reopen C6 for it.

---

## Phase 3 — Email templates (V1 = 5, per PM+Backend 2026-07-21)
- [ ] E1. Points earned
- [x] **E2. Expiring soon — CLOSED ✅ 2026-07-31.** Single reminder (not T-7/T-1); backend `cashback:wallet:expiry-reminder` fires once at CMS `days_before`. Copy uses `{{days_remaining}}` + `{{expires_at_long}}`, so nothing hard-promises an offset.

  **Deliverables**

  | asset | notes |
  |---|---|
  | [email-E2-expiring-soon.html](email-E2-expiring-soon.html) | production HTML — tables, inline CSS, VML button, dark-mode meta, merge fields, plain-text alternative, all build/send notes in the trailing comment |
  | [email-E2-hourglass-tick.gif](email-E2-hourglass-tick.gif) + [.py](email-E2-hourglass-tick.py) | **hero animation, transparent** · 186×320 shown at 93×160 · 3 frames · exactly 1000ms · 6.6KB |
  | ~~email-E2-hourglass-tick-shadow.gif + .py~~ | **DELETED 2026-08-04** — baked purple was wrong once the band became a gradient, and it had no transparency to salvage. Its generator could not run either (`hg_full.png`/`hg_empty.png` gone). Recover from git if ever needed |
  | [email-E2-token-shine.gif](email-E2-token-shine.gif) | **orphaned but KEPT** — shine sweep for the Rewards Token, superseded when the hourglass replaced it as hero. Unregenerable (`tile480.png` gone), so kept in case E1/E6/E7 wants an animated token. Its `.py` was **deleted 2026-08-04** — inert without the plate |
  | Figma `998:9377` | owner's design (current direction) |
  | Figma `1004:21751` | **email-safe build** — sits directly right of `998:9377` with BEFORE/AFTER labels |
  | Figma `989:21743` | earlier token-hero version, superseded |

  **Design decisions worth keeping**
  - Format came from the Beyond The Vines reference: one screen, brand-flooded background, number as a dark slab, one CTA. Adapted to the locked system — **purple flood because purple = poin** (gold would have read PRIME).
  - **Owner's persuasion framing:** *"people don't want to save the points, they fear not utilizing the points they earned as loss."* That killed two of my proposals — a "sekali top up aja, poin kamu aman lagi" line (preservation only defers the anxiety 60 days) and a value-translation line + priced-nominal grid (cut to keep it one screen). **These are the first things to add back if click-to-purchase underperforms.**
  - **Animation:** only the **neck stream** moves — a grain crossing the waist is the one cue that reads as "falling"; blinking arbitrary pixels reads as a glitch. Two-slot (the third grain was tried and cut as "too cramped"). Cadence **600ms rest + 2×200ms** — long stillness then a quick discrete change is what reads as *ticking* rather than a loading spinner. Frame 1 is the artwork as drawn, so **Outlook's first-frame-only rendering degrades to the correct static hero**.

  **Owed to whoever builds/sends it**
  - `days_before` = **7** — the wallet flips to amber urgency at ≤14 days, so a 7-day send lands the click-through on the matching amber state.
  - **Set a minimum-balance floor.** "40 poin kamu hangus" cheapens the program and burns deliverability — and since E3 was dropped, this is the **only** expiry email, which is also why the exact date is stated rather than "segera".
  - **The purple card cannot be CSS** (2 gradients + 3 inner shadows + grain + r28). Ship as a **background image behind live text** — VML for Outlook, `background-image` elsewhere — and **set `bgcolor="`Rewards 4`"` on that cell** or Outlook's image-blocking leaves `#4C1894` text on nothing.
  - **Every rounded corner renders square in Outlook.** VML roundrect for the CTA; ship the 24×24 step badges as PNGs; the two big cards are fine square.
  - **Barlow won't load in Outlook/Gmail** → Arial Bold. Check the 36px headline still wraps to 2 lines at Arial's wider metrics.
  - **Logo placeholder** — no RRQSHOP wordmark exists in the Figma file yet (only the old `logo / rrqtopup` `379:12964`). Header bar is white because the supplied wordmark has near-black "RRQ".
  - Social: **Instagram only**; WhatsApp is CS and sits on its own labelled line.

  **Undecided (minor, owner never called them)**
  - Slab reads "RRQSHOP Rewards Poin" where the locked unit convention everywhere else is just **"poin"** — this would be the only surface breaking it. A comment marks the one line.
  - **⏱️** renders inconsistently across clients and two subject emoji nudge spam scoring — 📢 alone is safer.
  - "poin**mu**" vs the headline's "poin **kamu**" mixes suffixed and separate possessive in one email.

  **Two traps if either GIF is ever regenerated**
  - **`disposal=2` is required** for the transparent version. With `disposal=1` the previous frame's grain persists through the transparent areas and nothing appears to move.
  - **Flood fill must be 4-connected.** The grain's bottom-left corner is diagonally adjacent to the waist outline, so 8-connectivity leaks out and eats the hourglass. (A hardcoded rect was the original bug — the 2x rescale rounds grains to 13/14/15px, so a fixed 14×14 rect left an L-shaped black residue.)

  ⚠️ **One accepted inaccuracy:** "Buruan pakai poinmu" implies *spending* saves the balance, but **redemption never extends `expires_at` — only earning does.** Someone who spends part still loses the rest on the same date. Owner chose utilisation framing over preservation deliberately; the extension rule sits in the footer fine print.
- [ ] E4. Redeemed receipt
- ~~E5. Guest "claim your points"~~ — **DEFERRED** (tied to R3)
- [ ] E6. Welcome + first point
- [ ] E7. Cashback Event announcement / coupon granted

---

## Totals
**V1 active:** 23 page/surface items + 6 components + **5 emails** (E3 expired dropped 2026-07-21; **E2 CLOSED 2026-07-31**, 4 remain) + rule-decisions.
**Deferred to later phase:** R3 guest escrow/OTP-claim, #10 guest claim CTA, E5 guest claim email. (Research preserved — Codashop blueprint + PDP analysis ready when picked up.)

## Status snapshot (2026-07-29, v5)

**Three deltas since v3. Full detail in the START HERE block; this is the index-level summary.**

**1. Phase 0 CLOSED.** Publisher-compliance copy rule locked → [publisher-compliance-copy-rule.md](publisher-compliance-copy-rule.md). Spine: no price claim whose comparison set is unbounded or includes the publisher's official channel. `termurah`/`harga terbaik` out, `murah`/`hemat`-with-named-mechanism in. Live-site audit found **11 failing strings, all keyed to i18n** → lintable in CI. **The design law that came out of it:** say what we give back (poin), not what the price beats — which is Rewards' whole job, so compliance and the retention story are the same edit.

**2. Hero motion shipped as code.** `rewards-hero-{warp.js, icons.js, warp.css, copy.css, warp.html}` in this repo root: inward ray field (79 rays, owner-tuned) + 6 floating props measured off the Nitro reference + full copy layer with an SVG stroke+inner-shadow filter + mobile at 375×290 via centre-crop (no new assets). Harness: 7 layer toggles, 3 slider panels, drag-to-place. FE handoff → [rewards-hero-motion-handoff.md](rewards-hero-motion-handoff.md). **Not integrated — no application code in this directory.**

**3. PRIME reposition (#16) landed as design.** Owner rewrote both breakpoints (desktop `858:17959`, mobile `858:18217`); copy parity exact; **PRIME's blanket discount is retired in favour of the poin multiplier**; desktop Panduan Poin modal built (`964:19271`). Diff → [prime-landing-figma-vs-prod.md](prime-landing-figma-vs-prod.md). Remaining work is non-design: subscriber notice, T&C, grandfathering, and the F7/F8/`saved_price_product` sweep.

### Item rollup (v5 — supersedes the v3 lines)
**DONE ✅:** brand (name/glyph/copy laws) · strip 3-state · nominal card system + Figma set (24 var) · homepage tile badge 3-state · event visual system · **checkout complete** — confirmation drawer set (**9 var**, `state` x `pakaiPoin`) + redeem toggle **5 states** + payment-extra strip · **desktop parity (2026-08-20): banner set `1873:26031` · payment set `1888:26536` · confirmation overlay `1894:26082`** · **colour styles: 15 local `Rewards/*` on top of the 2.0 library's 18, board `1911:26193`** · order status (mobile sticky + desktop left-col) · account (regular + PRIME) · **wallet** main + empty + debt (auto-layout wrapper, 351w, +purple sign convention) + Informasi Poin TL;DR panel (expanded 732:16577 / collapsed 729:18311) · **Panduan Poin — drawer `725:16668` (M) + modal `964:19271` (D)** · coupon banners #23 (green/purple/combo 635:12570) + PRIME-coupon popup (629:15489) · **ALL Phase-0 rules incl. publisher-copy** · **hero motion as shipping code** (5 files, repo root).
**PARTIAL [~]:** **#16 PRIME landing — design-complete both breakpoints; what's left is non-design** (subscriber notice / T&C / grandfathering for the retired discount) · **#17 PRIME account status — original "show multiplier" brief already done by #13; repurposed to (a) the missing subscription-management/cancel surface and (b) the benefit-change notice** · #1 homepage (header chip C1 + value-prop copy — kill list now concrete) · #25 new-member (success celebration + wallet entry; +Rule 1.10b gating for dev) · #27 coins-only (success variant) · #14 wallet (desktop layout + near-expiry ≤2wk amber) · #21 refund/clawback (debt wallet done; other clawback states left).
**DROPPED:** ~~#18 first-time explainer modal~~ (owner: users don't read; mechanics already shown by the redeem-toggle price re-render — see #18).
**UNTOUCHED:** ~~#19 error states~~ ✅ **built 2026-08-03** (board `1158:23243`, 3 states + 1 cut as a dev rule; both open rules decided) · **#20 T&C page — 4 queued inputs** (product-group rate table + full S&K · **non-redeemable product groups** — not the retracted DANA exclusion · reseller/pricing statement · PRIME benefit terms incl. grandfathering) · #22 event landing · ~~#28 pending-stale~~ ✅ closed (states already existed; rule written) · #29 PRIME bonus lots (row done in ledger) · C3 earn-preview (**audit before building** — may already exist inside the built drawer/redeem-toggle) · **4 emails (E1,E4,E6,E7)** — ~~E2~~ ✅ done, production HTML at [email-E2-expiring-soon.html](email-E2-expiring-soon.html).

**⏸ PARKED (owner, 2026-07-31):** **#24 My Coupons + C6 coupon card** — pages and card set were built, then all deleted at owner's request: *"don't change this area at all."* **Nothing remains on canvas; the PRIME Voucher page `972:13189` is untouched.** Findings kept under C6. · **Purple Active state** on the Rewards nav tab (see the GLOBAL NAV DECISION block).

### Session log — 2026-07-31 (chrome rebuild + #17 + C4)
**Chrome, all four built** (nav decision): `Header / M` `1014:21813` (boolean `Show PRIME`) · `Header / D` `1020:22750` · `Bottom Bar / M` `1026:22496` · `Sandwich Menu / M` `1036:22248` · plus `Logo / RRQSHOP` `1017:21865` (vector), `Header Button / PRIME` `1012:21825`, `Header Menu / D / Rewards` `1019:22165`, and Rewards tab variants on the local mobile set `92:9393`. Motion handoff re-targeted to the nav tab → [rewards-button-motion-handoff.md](rewards-button-motion-handoff.md).
**#17 CLOSED:** `PRIME Status Block / M` `1089:12588` · `PRIME Account Row` `1097:22032` · `PRIME History Row / M` `1112:12586` (sukses/pending/gagal) · all 10 rows of placeholder data fixed · 10 hidden `GoPay` leftovers deleted · layer names cleaned.
**C4 CLOSED:** `Riwayat Poin / Hangus` `1127:22108` (the real gap) + `Empty State / M` `1120:22067` trimmed to `kupon-kosong`. Two briefed "components" turned out to already exist or to be the wrong shape.
**🔁 Pattern worth remembering:** three items this session (#17's post-cancel, C4's "no points yet", C6's checkout state) were briefed as *"add a component"* when the real gap was *"no screen shows this state"* — or the state already existed elsewhere. **Check the brief against real surfaces before building.**
**CROSS-CUTTING SWEEP owed** (from the PRIME reposition + compliance audit — these live in the i18n bundle, not in Figma): `saved_price_product` "Diskon PRIME" → gold 3X chip + strikethrough poin pill · F7 `suggest_title` · F8 `suggests_desc` · `benefits_best_price` + desc · `app.title` · game `seo_title`/`seo_description` · `faq_answer_1` · **`TERMURAH` product tag → `HEMAT`** (CMS **image**, so no lint or grep will ever catch it).
**Figma index → [HANDOFF.md §9](HANDOFF.md).** Not restated here (deduped 2026-08-20): a second copy of 180+ node IDs is how they drift. HANDOFF §9 is authoritative; IDs that appear in this doc are **history inside session logs**, not a lookup table.
**Rare toggle edge states (spec-only, no render):** ~~remainder-below-method-minimum~~ **PROMOTED 2026-08-20 — this is now the ONLY valid instance of a method disabled by a redeem** (it inherited the job when the DANA rule was retracted), and production already ships the treatment; the mock just has no VA/Convenience-Store rows to draw it on · clawback-debt block · 100%-voucher block. **The latter two are a different class** — account states, not standing properties, so each needs its own reason string.

### Session log — 2026-08-20 (desktop parity + colour styles + one retraction)
**Desktop parity, three items, each a different answer.** `Rewards / Desktop` `1873:26031` (4 var — banner is the *top edge of the step-1 card*, so content is centred and the radius is `12 12 0 0`; sample frame in `Game Page` `1869:31245` replaced by instance `1875:14181`) · `Pilih Pembayaran / Desktop` `1888:26536` (5 var — **built twice**: v1 cloned the game-page panel `1869:31735` and was scrapped because that panel matches neither production nor mobile; v2 is mobile's accordion with rows two-per-line) · `Konfirmasi Pesanan - D (modal overlay)` `1894:26082` (**no variant set needed** — every confirmation in the 2.0 file is 375 wide, including inside the 1440 desktop frame `164:7734`, so the drawer is breakpoint-agnostic and only the scrim/centring was missing).
**Colour styles, as an addition not a fork.** The 2.0 library (`383:12517`) publishes 18 flat styles and they are **already bound** in the 2.5 file as REMOTE styles — so six styles created here were deleted as duplicates (`Primary`, `Primary Gradient`, `Success`, `Surface 7`, `Surface 6`, plus the red). 15 remain: `Rewards/Rewards 1–5` (purple scale, dark→light — the library has no purple) · `Rewards/Surface 1–7` (tinted grounds, 1–3 poin / 4–5 boost / 6–7 PRIME) · `Green Highlight` · `Near Expiry` · `On Gold`. **Nothing is bound yet — the rebind sweep is the next real step**, and it also fixes the PRIME-tile defect (`Surface 6` is correct for rows drawn on `Surface 3`).
**🚨 The retraction.** The DANA x poin-ON rule never existed — it had hardened into the `632:12570` set description as "DANA RULE (locked)" and into 12 places in HANDOFF.md. Undone in `631:12571`, `1888:26058` and the description. Eligibility is a **per-product/game backend flag**; the surviving mechanism is a **method minimum tested against the remainder after poin**.
**🔁 Pattern worth remembering (and it fired twice this session):** *check the mock against production before building the desktop of anything.* The payment set was built once from a mock panel that was wrong, and my own two follow-up conclusions (collapsed groups need a signal; disabled rows drop their price) were both overturned by a single production screenshot. Production is the reference; the 2.5 mocks are behind it in at least four places on the payment step alone.

## Top design risks to watch
1. **Guest bounce** — guests can't earn; homepage + checkout must convert without feeling like a paywall.
2. **Delivered-to-earn gap** — points not instant; success + pending pages must set expectation.
3. **A method that cannot take the remainder** — when redeeming drops the remainder under a method's minimum (VA and Convenience Store rows), that method needs a visible disabled state, not a silently missing control. Production already does this; the mock has no VA/CS rows to show it on. ~~DANA redeem exception~~ retracted 2026-08-20.
4. **Expiry clarity** — extend-on-purchase is subtle; wallet must surface it or retention mechanic is invisible.

## Build order
R1 + R3 + Phase-0 rules gate everything → then Phase 1 core earn/redeem (1–9) → account/wallet (13–15) → PRIME (16–17) → support states + emails.
